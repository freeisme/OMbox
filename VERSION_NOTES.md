# 版本更新说明

本文件记录每个可部署版本的功能变更、数据库影响、配置要求和回滚提示。
可部署版本必须使用未占用的 SemVer 注释标签，并在标签对应提交中包含本文件的同名
版本标题，例如 `v1.1.0`。

写作约定：

- 只写本次改了什么、有什么影响、怎么验证、怎么回滚，**保持简洁**，不复述实现细节；
- **不出现任何真实业务数据**——人员姓名、工号、设备编码、组织与仓库名称、客户名称一律用
  泛化示例（如「某部门」「IT-PC-01」），详细来龙去脉写到 `docs/development/` 下的文档里；
- v2.31.0 起旧前端已整体删除，早期条目里的「回滚到旧前端 / 从 `MIGRATED_VIEWS` 移除」
  操作已不再适用。

## v3.0.0

发布日期：2026-09-21

### 变更

- **更名**：项目更名为 **OMbox · 运维（O&M）盒子**；系统设置里的默认系统名同步改为
  「OMbox运维（O&M）盒子」（`database/bootstrap/19_auth_and_settings.sql` 的 `app_name` 初值）。
- **仓库重建**：以单个初始提交重建仓库，历史中不再保留早期提交里的真实业务数据；
  版本号从 v3.0.0 重新起，旧版本标签全部作废。
- **README 重写**：按 OMbox 重新介绍定位、能力、部署方式与文档入口。
- **测试数据约定**：测试与示例一律使用虚拟信息（张三 / 李四 / 测试部门 / IT-PC-01 等），
  不引用真实人员、设备编码、组织与仓库名称。

### 数据库影响

无结构变更。默认系统名只影响**全新部署**；已有库沿用 `system_setting.app_name` 里的现值，
需要改名时在「设置 → 系统设置 → 系统名称」修改。

### 验证

脱敏扫描通过；单元与结构回归通过；`vue-tsc` 与 `vite build` 通过；按 Docker 初始化流程
重建空库核对：仅权限字典与参考数据有内容，人员、终端、账号、审计日志等业务表均为 0 条。

### 回滚

历史已重建，无法 `git revert` 回到 v2.x；回退需部署旧版本包并从备份恢复数据库。

## v2.31.2

发布日期：2026-09-21

### 变更

- **文档脱敏**：清理 `VERSION_NOTES.md` 里引用的真实业务数据（人员姓名、设备编码与设备名、
  组织与仓库名称），改为泛化示例；前端新增终端的示例设备名一并改成通用示例。
- **脱敏扫描加固**：`tools/scan_release_safety.py` 新增手机号、身份证号、邮箱与资产编码
  四类规则；`docs/development/release-safety.md` 补充「文档与更新公告」纪律和空库验收方法。
- **更新公告约定**：版本说明只写变更、影响、验证与回滚，保持简洁；实现细节写进
  `docs/development/`。

### 数据库影响

无。

### 验证

脱敏扫描通过；单元与结构回归 137 项通过；按 Docker 初始化流程重建空库核对：新库只有
权限字典与参考数据（72 张表 / 19 张有数据），人员、终端、账号、审计日志等业务表均为 0 条。

### 回滚

`git revert` 本提交即可。

## v2.31.1

发布日期：2026-09-21

### 变更

- **IT物资目录**按层级展示并修正排序：三层各自排序（类型按名称，品牌/型号按 `sortOrder` 再按名称）；
  修正 `el-table` 因跨层主键撞号导致的层级错乱；缩进真正生效并调整为每级 32px；
  默认展开到型号，筛选时自动展开命中分支。
- **使用人员**终端不再整页下载：改用 `/api/list/computers?fields=names` 精简投影，
  设备弹窗按 `userId` / `unassigned` 按需取数（页面载荷 226 KB → 106 KB）。
- **物资流转、离职人员、基础字典**补齐 `include=` 字段裁剪（整包 549 KB → 112 / 102 KB）。
- **侧栏菜单**改为用 Element Plus 菜单变量着色，修复深色侧栏出现白底灰字。
- **使用人员行高**回到 32px：操作列过窄导致三个按钮换行，列宽已加宽到 210px。
- **窄屏（<768px）**隐藏筛选标签、改用 placeholder 说明，筛选区高度降 60–180px。
- 内部清理：删除失效的 `.oa-baseline-row` 样式与空字段，修正 `oa-tabular` 类名不一致。

### 数据库影响

无。本版本不改数据库结构，不需要执行迁移。

### 验证

单元与结构回归 137 项通过；`vue-tsc` 与 `vite build` 通过；浏览器端到端 16 项断言通过
（三层缩进与父子顺序、侧栏配色、行高、精简接口与 `include=`、四档断点无横向溢出）。

### 回滚

`git revert` 本提交即可；接口新增的 `userId` / `unassigned` / `fields` 均为可选参数，旧调用方不受影响。

## v2.31.0

发布日期：2026-09-21

### 变更 1：前端收敛完成（设计计划阶段 2 收尾 + 阶段 3 + 阶段 4）

**阶段 2 收尾**：使用人员、IT物资、设置三页改用 `PageHeader + FilterBar + DataPanel`
统一骨架；`el-card` 直接拼装的页面不再存在。

**阶段 3 弹窗与抽屉统一**：新增 `FormDialog`、`DetailDrawer` 与 `composables/useConfirm.ts`，
替换 22 处弹窗、6 处抽屉与 8 处危险操作确认。

- 弹窗宽度收敛为 sm(420) / md(560) / lg(720) / xl(900) 四档，footer、loading 与禁用态统一；
- 抽屉宽度按 XL/L 给 560–760px，M/S 自动全宽；
- 危险操作（报废、离职、删除仓库）统一红色确认按钮，取消与关闭一律返回 false，
  调用方少写一层 try/catch。

**阶段 4 性能与响应式**：

- Element Plus 改为按需引入（`unplugin-vue-components` + `unplugin-auto-import`），
  路由全部懒加载；首屏 JS 从 1309 KB 降到 **268 KB**（gzip 114 KB），
  首屏 CSS 从 381 KB 降到约 66 KB。
- 新增 `GET /api/list/computers`：办公终端列表的服务端分页与过滤
  （`page`/`pageSize`/`keyword`/`status`/`orgId`，`pageSize=0` 取全部供导出与全选）。
  264 行数据每页只传约 28 KB，筛选与翻页都由服务端完成。
- `GET /api/state` 新增 `include=a,b,c` 字段裁剪：办公终端页 549 KB → 91 KB，
  IT物资页 → 45 KB，使用人员页 → 226 KB。
- 四档断点落地（XL ≥1200 / L 992–1199 / M 768–991 / S <768）：小屏导航转为顶部横向条、
  筛选与工具条单列堆叠、弹窗近满宽、抽屉全宽；1440/1100/900/420 四档实测无横向溢出。
- 视图内联 style 片段从 364 处降到 **33 处**（另有 12 处动态 `:style` 用于机房画布定位，
  由运行时坐标决定，不属于硬编码样式）。

### 变更 2：删除旧前端死代码

- 删除 `web/app.js`、`web/styles.css`、`web/index.html`；
- 删除 `frontend/src/views/LegacyPage.vue`、`frontend/src/components/LegacyFrame.vue`、
  `frontend/src/legacyBridge.ts`；
- `server.py` 去掉 `LEGACY_PREFIX`、`/legacy/` 前缀重写与 `_legacy_request` 标记，
  `X-Frame-Options` 恒为 `DENY`，Content-Security-Policy 不再有 `frame-ancestors 'self'` 分支，
  `send_spa_index` 不再回落到旧入口；
- 顶栏「消息」按钮改为跳转 `/service-management`（未读计数走 `/api/notifications`），
  「重新加载」改为整页刷新；
- `web/` 只保留 `app/`（构建产物）与 `assets/device-images/`（面板图上传目录）；
- 结构回归中 39 处针对旧前端的断言改写为针对 Vue 实现或删除，新增
  `FrontendLegacyRemovalTests` 守住「旧文件不存在、服务端无 legacy 分支、Vue 源码无桥接引用」。

### 变更 3：修复登录超时不会跳回登录页

原来只有首次进入时校验一次会话，之后接口返回 401 只弹错误提示，页面停留在
「看起来已登录、但所有操作都失败」的状态。现在：

- `api/client.ts` 对非 `/api/auth/*` 的 401 触发 `setUnauthorizedHandler` 注册的回调；
- `main.ts` 清空会话、提示「登录状态已过期，请重新登录。」，并跳转
  `/login?redirect=<原地址>`，带防重入保护；
- `/api/auth/*` 的 401 仍然交给路由守卫，不影响登录页自身的探测。

### 数据库影响

无。本版本不改数据库结构，不需要执行迁移。

### 配置要求

- 前端新增两个开发依赖：`unplugin-vue-components`、`unplugin-auto-import`，
  已写入 `frontend/package.json` 与 `pnpm-lock.yaml`；Docker 多阶段构建用
  `pnpm install --frozen-lockfile` 即可，无需额外配置。
- 无新增环境变量。

### 验证

- 单元与结构回归 136 项通过；`vue-tsc --noEmit` 与 `vite build` 通过；脱敏扫描通过。
- 浏览器端到端（Edge 无头，本地排练库）：18 个页面标题正确、无 JS 异常；
  办公终端首页 50 行、共 264 条，翻页与状态筛选（→239 条）都打到服务端；
  表格行高实测 32px；会话过期后自动跳转 `/login?redirect=/computers`；
  1440/1100/900/420 四档无横向溢出。
- 使用人员表沿用「姓名 + 工号」双行单元格，行高约 41px，其余列表统一 32px。

### 已知取舍

- 使用人员与 IT物资未改服务端分页：前者要按组织树统计人数、还要在离职转交与设备弹窗里用
  全量数据，后者是目录树；两者改为请求裁剪后的状态快照。
- 小屏筛选区为单列堆叠，未做「更多筛选」折叠。

### 回滚

`git revert` 本提交即可，业务逻辑与接口契约未改动；旧前端代码随本版本删除，
回滚会一并恢复到 `web/` 下。

## v2.30.0

发布日期：2026-09-21

### 变更：列表页统一到同一骨架（设计计划阶段 2）

- 8 个列表/管理页改用 `PageHeader + FilterBar + DataPanel` 统一结构：办公终端、物资流转记录、
  报废记录、工单、基础字典、同步与质量、服务管理、机房巡检。
- 页头统一为「标题 + 一句话说明 + 右侧操作」，筛选区统一由 `FilterBar` 承载
  （结果计数固定右下、有筛选条件时显示「清除筛选」），数据区统一 `DataPanel`
  （边框 + 头部标题/提示 + 加载态 + 底部分页插槽）。
- 「同步与质量」把质量问题筛选器移到面板头部、分页移入底部插槽；「机房巡检」「服务管理」
  的标签页移入页头筛选区，页面层级更扁平。
- 新增公共类 `oa-filter-form`、`oa-full-width`、`oa-muted`、`oa-hint`、`oa-flex-between`、
  `oa-flex-row`，替代各页重复的内联样式。
- 内联样式总量从阶段 1 后的 279 处降到 **218 处**（相对最初的 364 处减少 40%）；
  表格行高全站统一 32px。

### 尚未完成（阶段 2 收尾）

- 使用人员、IT物资、设置三页尚未改造：前两页正在被并行会话改动，设置页有 1272 行，
  留到下一批以避免互相覆盖；机柜视图、设备面板、网络拓扑、设备台账属于画布类页面，
  内联样式多为动态定位，不在此次改造范围。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查、Vite 构建、脱敏扫描通过。
- 浏览器端到端：8 个页面逐一访问，页头标题正确、表格行高实测 32px（有空数据的页面为 n/a）、
  无旧前端 iframe、无 JS 报错；办公终端 50 行、工单 2 行、基础字典 13 行、同步与质量 20 行
  均为真实数据。
- 回滚：`git revert` 本提交即可，业务逻辑与接口未改动。

## v2.29.0

发布日期：2026-09-21

### 新增：设计令牌与共享页面组件（设计计划阶段 1）

- 新增 `frontend/src/styles/tokens.css`：间距（4 的倍数）、字号（12–28）、行高、圆角、边框、
  面层、外壳尺寸、表格密度等令牌；`app.css` 移除重复变量，只保留外壳样式与公共类。
- **表格改为紧凑密度**：统一 `--oa-row-height: 32px` 行高（DOM 实测 32px），一屏可多显示约 25% 行；
  表头加底色、数值列使用等宽数字。
- 新增 `frontend/src/components/ui/` 七个共享组件：`PageHeader`、`DataPanel`、`FilterBar`、
  `ToolbarActions`、`StatusTag`、`EmptyHint`、`MetricCard`；颜色与间距全部取自令牌。
- 工作台、操作日志、离职人员三页完成迁移：**内联样式 86 处 → 1 处**，其中工作台 62 → 0、
  操作日志 12 → 0；全站内联样式从 364 处降到 279 处。
- 状态标签统一走 `StatusTag`（复用 `labels.ts` 的文案与颜色映射），空态统一走 `EmptyHint`；
  工作台待办项补充 `role`/`tabindex` 与回车、空格触发，键盘可用。

### 设计约定（后续页面必须遵守）

- 间距只用 `--oa-space-*`，字号只用 `--oa-font-*`，不再写裸 px 与十六进制色值；
- 一级容器统一 `DataPanel`，不再嵌套卡片；列表页骨架固定为
  `PageHeader → FilterBar → ToolbarActions → DataPanel → Pagination`；
- 空数据用表格 `empty-text`，非表格空态用 `EmptyHint`；页头一律 `PageHeader`。

### 验证与回滚

- 单元与结构回归 142 项、`vue-tsc` 类型检查、Vite 构建、脱敏扫描全部通过。
- 浏览器端到端：工作台页头显示问候语与「待办 3 项 · 未读通知 0 条」、9 个面板、4 张指标卡
  （264 台 / 在用 91% · 239 台在用）、3 条待办；操作日志显示 1248 条；离职人员 15 人；
  三页表格行高实测均为 32px；无 JS 报错。
- 回滚：`git revert` 本提交即可；令牌与组件为新增文件，业务逻辑与接口未改动。

## v2.28.0

发布日期：2026-09-20

### IT物资：类型 / 品牌 / 型号的显示统一，并可在页面上维护

原来列表把「入库日期 / 配置 / 批次」拆成三列，只有电脑类物资有值，其余物资整行都是「—」，
层级之间单位也不一致（类型显示「台」、品牌和型号显示「件」），看起来参差不齐。现在：

- 表格只保留「层级 + 类型 / 品牌 / 型号」「库存数量」「操作」三列：
  类型行给「N 个品牌 / M 个型号」，品牌行给「N 个型号」，型号行只显示型号；
  配置 / 批次 / 入库日期合并成型号行下面的一行小字，且**只对电脑类物资显示**；
- 类型、品牌、型号三层都带上类型自己的计量单位，不再出现「台 / 件」混排；
- 层级改用树节点上的 `level` 字段判断，不再靠「有没有单位」猜层级；
- **品牌即使当前仓库没有在库型号也保留**，否则新建的品牌在列表里会直接消失；
- 恢复旧前端的目录维护能力：类型行可「编辑类型 / 新增品牌」，品牌行可
  「编辑品牌 / 新增型号」，型号行可「编辑 / 入库 / 调拨」；
  型号维护里的数量按差值走 `POST /api/inventory/receipts`（增加）或
  `POST /api/inventory/adjustments`（减少），不直接改库存字段；
- 顶部增加「物资类型 / 品牌」两个筛选下拉与「清除筛选」，与关键字搜索叠加。

### 使用人员：人员清单列间距

- 人员清单每列加宽（人员 / 组织部门 / 岗位 / 状态 / 名下设备 / 操作），
  单元格左右内边距统一为 14px、上下 10px，操作列的三个按钮之间留出间距。

### 设置页完整迁移到 Vue（不再使用旧前端 iframe）

设置页原来下半部分是 `<iframe>` 承载的旧设置页，现在全部用 Vue 重写，分为六个分区：

| 分区 | 内容 |
| --- | --- |
| 系统参数 | 系统名称、会话时长（1-168 小时）、登录页提示语 |
| 安全与密码 | 修改当前账号密码（校验长度与两次输入一致） |
| 数据库备份 | 自动备份计划、立即备份、备份列表与密码二次校验下载 |
| 账号管理 | 账号列表、新增/编辑账号（角色、绑定人员、启停、重置密码） |
| 角色与权限 | 角色 / 用户覆盖两种配置对象、模块 × 操作权限矩阵 + 数据范围、创建角色 |
| 系统更新 | 更新来源（GitHub / 自定义地址）、更新通道、检查版本、选择目标版本、版本说明、执行更新 |

顺带修掉一个显示问题：`/api/access-control` 返回的角色主键是数字、用户主键是字符串，
前端统一转成字符串后再绑定下拉框，否则「目标对象」会显示成原始 id（例如 `3`）。

数据库影响：无（只改前端页面，全部复用既有接口）。
回滚提示：回退到 v2.27.0 即可；设置页会回到「上半部分 Vue + 下半部分 iframe」的形态。

## v2.27.0

发布日期：2026-09-20

### 新增：使用人员清单恢复"编辑人员名下设备"

旧版前端在人员清单里可以直接维护该人员名下的设备，迁移到新版 Vue 前端后这个入口
丢失了（只剩只读的"名下设备"列）。现在按旧版功能恢复，并改为调用正式的命令接口。

在「使用人员」列表的操作列新增 **设备** 按钮，打开「设备清单 · 姓名」弹窗，包含：

- **办公终端**：列出名下终端（设备名、品牌型号、状态），可 **解除** 分配；
  下方下拉选择空闲终端后 **分配** 给该人员；
- **显示屏 / 非资产设备**：逐条列出，选中回收仓库后可直接 **回收** 并回补库存；
- **回收设置**：统一选择回收仓库与操作备注，解除与回收都会带上；
- **领用设备 / 物资**：选择类型（显示屏 / 非资产设备）、出库仓库、库存型号（显示可用
  库存）与数量后登记领用。

细节处理：

- 显示屏类型用**类型编码 `monitor`** 判断，而不是物资类型主键，避免类型 id 变化后
  显示屏列表取不到型号；
- 显示屏按单台登记（后端也只接受数量 1），切换领用类型会清空已选型号与数量；
- 离职 / 停用人员只允许查看与回收，分配与领用按钮置灰，与后端校验保持一致。

数据库影响：无（仅前端页面，复用现有命令接口）。
回滚提示：回退到 v2.26.0 即可，不涉及数据迁移。

## v2.26.0

发布日期：2026-09-20

### 修复：巡检"填写完还提示有未检查"

根因有两条：

1. **判定"已检查"的唯一依据是该行保存了结论**（正常 / 异常 / 不适用）。
   只在输入框里写实测值或说明不算已检查；而模板里的「本次巡检发现的问题与处理建议」是文本型事项，
   最容易出现"写了字但没选结论"。
2. **前端把"异常"选项的值写成了 `abnormal`，而后端只接受 `fail`**，
   所以选"异常"保存会被拒绝（400），那一项永远停在未检查。

修复内容：

- 修正「异常」选项值为 `fail`，与后端 `CHECK_RESULTS`（pending / ok / fail / na）一致；
- **未检查的项不再预选"正常"**，下拉框显示"请选择结论"，避免"看着已填其实没保存"；
- 每个检查项右侧显示状态标签：未检查 / 未保存（改了没点保存）/ 已保存；
- 抽屉顶部显示「共 N 项 · 未检查 N · 未保存 N」；
- 点「保存」时若没选结论、或选了异常却没填说明，直接给出提示；
- 点「提交巡检」前先检查未保存结论的项，按事项名称提示（最多 5 项），不再到提交时才报错；
- 后端提交校验的报错改为**列出未检查 / 缺说明的事项名称**，不再只报数量；
- 旧前端（`/legacy/`）同步做了同样的修复，保持一致。

### 优化：IT 物资列表显示

- 类型 / 品牌 / 型号三层每行加层级标签，型号行不再和品牌行混在一起辨认；
- 库存数量列右对齐、等宽数字，层级行显示"共 N 件"以区分合计与单型号库存；
- 入库日期 / 配置 / 批次只在型号行显示，层级行留空，去掉整屏的"—"噪音。

### 验证

- 结构回归 `python -m unittest tests.test_regressions` 全部通过（新增巡检交互与物资列表用例）；
- 前端 `vue-tsc --noEmit` 与 `vite build` 通过；
- 端到端回归全部复跑通过：巡检、机柜视图、设备面板与拓扑、机房设备导入、报废、安全。

### 回滚提示

- 本次不涉及数据库结构变化，回退代码即可；已提交的巡检表不受影响。

## v2.25.0

发布日期：2026-09-20

### 新增：机房巡检与同步与质量迁移到 Vue（全部页面迁移完成）

- **机房巡检 `/inspection`**：三个标签页 —— 巡检任务（列表含编号/时间、范围、模板、状态、巡检人、
  异常说明，支持发起巡检、查看详情、作废）、巡检模板（名称/编码/检查项数/启用）、
  机房与机柜（机房与机柜两张表）；任务详情抽屉按检查项逐条记录（结论/记录值/说明）并支持
  提交巡检（含异常说明与备注）。接口：`/api/inspection/sites|racks|templates|tasks`、
  `POST /api/inspection/tasks/{id}/items/{itemId}/check`、`.../submit`、`.../void`。
- **同步与质量 `/governance`**：同步暂存批次（来源、状态、总数/有效/无效/已应用、开始时间，
  支持应用未出错批次）与数据质量问题（按待处理/已解决/已忽略切换、关键字搜索、分页，
  支持运行质量检查、填写处理结果后解决、填写原因后忽略）。
  接口：`/api/sync-runs`、`POST /api/sync-runs/{id}/apply`、`/api/data-quality/issues`、
  `POST /api/data-quality/run`、`.../resolve`、`.../ignore`。

### 迁移完成度

- **16 个导航页面全部迁移到 Vue**：工作台、办公终端、使用人员、离职人员、基础字典、IT物资、
  物资流转记录、报废记录、机房巡检、工单、服务管理、同步与质量、操作日志、设置、设备台账、
  机柜视图、设备面板、网络拓扑（其中设置页的"完整设置"区域仍以 iframe 承载旧设置页）。
- 旧的 `/legacy/` 前端仅在该设置区域内继续使用，导航层面已不再需要它；
  后续把设置子标签迁完即可删除旧前端与 `window.oaLegacy` 桥接。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建、脱敏扫描通过。
- 浏览器端到端：`/governance` 无 iframe、质量问题列表正常列出（如「办公终端缺少序列号和
  固定资产编号」）、解决弹窗可用；`/inspection` 无 iframe、三个标签页齐全、
  当前测试库缺巡检表时显示空状态且不报错；两页均无 JS 报错。
- 回滚：把 `router/index.ts` 中 `inspection` / `governance` 从 `MIGRATED_VIEWS` 移除即可。

## v2.24.0

发布日期：2026-09-20

### 新增：工单与服务管理迁移到 Vue

- **工单 `/tickets`**：关键字（标题/编号/描述）、状态、只看我负责的筛选；列表显示编号、标题、
  状态、优先级、**SLA**（已超时 / 剩余分钟 / 未配置）、负责人、报障人、创建时间；
  详情抽屉展示状态、SLA、负责人、报障人、关联终端、描述与处理结果，支持：
  - 添加处理记录（`POST /api/tickets/{id}/notes`）；
  - 状态流转（`POST /api/tickets/{id}/transitions`），可选状态严格按后端
    `TICKET_TRANSITIONS` 生成，标记"已解决"时强制填写处理结果；
  - 新建工单（类型/影响/紧急度/优先级）。
- **服务管理 `/service-management`**：按权限显示标签页 —— 变更、问题、知识库、SLA 策略、审批、通知；
  切换标签按需加载。变更/问题/知识库/SLA 支持新建（字段与后端 `create_*` 契约一致），
  审批支持通过/驳回（驳回需填原因），通知支持标记已读；工单在独立页面处理。

### 迁移进度

- 已迁移到 Vue：工作台、办公终端、使用人员、离职人员、基础字典、IT物资、物资流转记录、
  报废记录、工单、服务管理、操作日志、设置、设备台账、机柜视图、设备面板、网络拓扑共
  **16 个页面**。
- 仍由旧前端在 `/legacy/` 渲染的只剩 2 个：机房巡检（`/inspection`）与同步与质量
  （`/governance`）。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建、脱敏扫描通过。
- 浏览器端到端：`/tickets` 无 iframe、列表与筛选正常、详情抽屉含处理记录与状态流转、
  新建工单弹窗可用；`/service-management` 无 iframe、六个标签页齐全、空数据时显示空状态；
  无 JS 报错。
- 回滚：把 `router/index.ts` 中 `tickets` / `serviceManagement` 从 `MIGRATED_VIEWS` 移除即可。

## v2.23.0

发布日期：2026-09-20

### 新增：IT物资页迁移到 Vue

- 新增 `/inventory`（Vue）：仓库切换标签（含"全部仓库"）、按仓库汇总的库存树
  （物资类型 → 品牌 → 型号，含数量/入库日期/配置/批次）、关键字搜索、展开/收起、
  导出当前仓库（CSV）与采购入库记录列表（分页）。
- 操作入口：**新增仓库 / 库存调拨 / ＋入库 / 新增类型 / 导出当前仓库**，均按权限显示。
  - 入库：`POST /api/inventory/receipts`（型号、仓库、数量、入库日期、来源、备注）；
  - 调拨：`POST /api/inventory/transfers`（型号、调出/调入仓库、数量、备注），
    前端先校验两仓库不能相同；
  - 仓库维护：`POST/PUT/DELETE /api/inventory/warehouses[/{id}]`，删除前二次确认；
  - 新增类型：`POST /api/resources/inventory-type`。
- 型号下拉同时显示所属类型、品牌与当前库存数量，便于选择。

### 迁移进度

- 已迁移到 Vue：工作台、办公终端、使用人员、离职人员、基础字典、IT物资、物资流转记录、
  报废记录、操作日志、设置、设备台账、机柜视图、设备面板、网络拓扑共 **14 个页面**。
- 其余 4 个页面（机房巡检、工单、服务管理、同步与质量）继续由旧前端在 `/legacy/` 渲染。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建、脱敏扫描通过。
- 浏览器端到端：`/inventory` 无 iframe；默认仓库名称、库存件数与采购入库条数正确显示；
  库存树正常渲染；关键字筛选后剩余库存与行数随之变化；物资入库、库存调拨、新增仓库
  三个弹窗均可打开；无 JS 报错。
- 回滚：把 `router/index.ts` 中 `inventory` 从 `MIGRATED_VIEWS` 移除即可回到旧前端渲染。

## v2.22.0

发布日期：2026-09-20

### 新增：物资流转记录与报废记录迁移到 Vue

- **物资流转记录 `/flow-control`**：关键字（物品、品牌、型号、备注、来源去向、关联人员）、
  物资类型、业务类型、业务分类、时间区间筛选，分页表格（时间、业务类型、业务分类、物资类型、
  品牌型号、数量、库存影响、调出方、接收方、关联人员、备注）与 CSV 导出；
  备注支持「修正」，走 `POST /api/inventory/movement-logs/{id}/note-corrections`，
  保留原始备注、更正内容与更正原因，列表显示已修正次数。
- **报废记录 `/scrap-records`**：关键字与类型（IT物资 / 办公终端）筛选、刷新、分页表格
  （报废时间、类型、物品、数量、使用人、报废原因、操作人、库存影响）与 CSV 导出，
  详情弹窗展示完整字段；数据来自 `GET /api/scrap-records`。

### 顺带修复

- 业务类型映射补齐 `inventory_allocation`（库存领用）与 `inventory_return`（库存归还）：
  这两个动作码在现网数据里各有 24 与 5 条，旧前端因映射表未覆盖会直接显示英文动作码，
  新页面已显示为中文并归入对应业务分类。

### 迁移进度

- 已迁移到 Vue：工作台、办公终端、使用人员、离职人员、基础字典、物资流转记录、报废记录、
  操作日志、设置、设备台账、机柜视图、设备面板、网络拓扑共 **13 个页面**。
- 其余 5 个页面（IT物资、机房巡检、工单、服务管理、同步与质量）继续由旧前端在 `/legacy/` 渲染。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建、脱敏扫描通过。
- 浏览器端到端：`/flow-control` 无 iframe、流转记录按时间倒序列出、首行显示时间 / 物品 /
  方向 / 来源与去向、关键字筛选生效、修正弹窗可打开；`/scrap-records` 无 iframe、
  空数据时显示空状态；无 JS 报错。
- 回滚：把 `router/index.ts` 中 `flowControl` / `scrapRecords` 从 `MIGRATED_VIEWS` 移除即可。

## v2.21.0

发布日期：2026-09-20

### 新增：使用人员页迁移到 Vue

- 新增 `/employees`（Vue）：左侧组织架构树（带每人数的层级计数、节点点击即按该组织子树筛选、
  全部展开/收起），右侧人员清单（人员/工号、组织/部门、岗位、状态、**名下设备**、
  编辑与办理离职）。
- 筛选：人员关键字（姓名、工号、部门、组织）、IT 物资关键字（品牌/型号，同时匹配名下办公终端）、
  人员状态（在职/停用/公用）、组织（含下级），一键清除筛选。
- 批量：全选当前结果、清空选择、导出选中人员（CSV，含名下终端与显示屏/非资产物资）。
- 新增/编辑人员走 `POST/PUT /api/resources/employee`，支持在职/停用/公用三种状态；
  公用人员会提示"用于挂靠非个人设备、不能绑定登录账号、工号留空按组织自动生成"。
- **办理离职**：先调用 `GET /api/employees/{id}/offboarding-preview` 取得名下资产与物资清单，
  弹窗内逐项选择处理方式（回收 / 转交他人 / 异常待处理），并按后端规则做前置校验：
  回收必须指定回收目标仓库、转交必须选择接收人（仅在职或公用人员）、异常待处理必须填写说明；
  提交 `POST /api/employees/{id}/offboard`，成功后自动刷新列表并清理缓存。

### 迁移进度

- 已迁移到 Vue：工作台、办公终端、使用人员、离职人员、基础字典、操作日志、设置、设备台账、
  机柜视图、设备面板、网络拓扑共 **11 个页面**。
- 其余 7 个页面（IT物资、物资流转记录、报废记录、机房巡检、工单、服务管理、同步与质量）
  继续由旧前端在 `/legacy/` 渲染。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建、脱敏扫描通过。
- 浏览器端到端：`/employees` 无 iframe；人员总数与组织树节点数正确；按姓名关键字筛选后
  命中数收敛，行内显示名下终端与非资产物资；离职弹窗标题带人员信息并逐项列出待处理资产
  （办公终端需选择处理方式、键鼠类默认回收并预选仓库）；新增人员弹窗字段完整；无 JS 报错。
- 回滚：把 `router/index.ts` 中 `employees` 从 `MIGRATED_VIEWS` 移除即可回到旧前端渲染。

## v2.20.0

发布日期：2026-09-20

### 新增：离职人员与基础字典页迁移到 Vue

- **离职人员 `/left-employees`**：关键字检索（姓名、编号、组织、部门、离职说明）、分页表格
  （人员、原组织/部门、岗位、离职日期、离职设备快照）与详情抽屉（人员编号、组织路径、部门、
  岗位、离职日期、归档时间、手机号、离职信息、备注，以及离职时设备快照明细与处置说明）。
  页面为只读，数据取自 `/api/state` 的 `leftEmployees`。
- **基础字典 `/dictionary`**：左侧组织树（含节点内「新增下级 / 编辑」）、右侧非资产设备类型表
  （编码、名称、计量单位、引用人数），统一搜索框同时过滤组织与类型；新增/编辑走
  `POST/PUT /api/resources/organization` 与 `POST/PUT /api/resources/inventory-type`。
  与旧版一致：组织与类型**不提供删除**（旧版对组织提示"不支持直接删除"、对类型提示
  "字典类型删除已暂时禁用"，避免绕过库存与审计事务），页面里已用说明文字标注。

### 迁移进度

- 已迁移到 Vue：工作台、办公终端、离职人员、基础字典、操作日志、设置、设备台账、
  机柜视图、设备面板、网络拓扑共 **10 个页面**。
- 其余 8 个页面（使用人员、IT物资、物资流转记录、报废记录、机房巡检、工单、服务管理、
  同步与质量）继续由旧前端在 `/legacy/` 渲染。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建、脱敏扫描通过。
- 浏览器端到端：`/left-employees` 无 iframe、显示 15/15 人、首行为真实档案（含办公终端与
  两台显示屏的设备快照）、详情抽屉字段完整、关键字「钱」筛选后 1/15；
  `/dictionary` 无 iframe、组织 71/71 与设备类型 13/13、组织树 71 个节点、类型表 13 行、
  关键字「显示」筛选出组织 0/71 与类型 1/13、新增组织弹窗可打开；无 JS 报错。
- 回滚：把 `router/index.ts` 中 `leftEmployees` / `dictionary` 从 `MIGRATED_VIEWS` 移除即可。

## v2.19.0

发布日期：2026-09-20

### 新增：办公终端页迁移到 Vue

- 台账页（`/computers`）由旧前端迁到 Vue：关键字检索（设备名、型号、固资编码、SN/ST、
  组织、使用人）、状态筛选、**组织树筛选（含下级）**、分页表格、批量选择与 CSV 导出。
- 行内操作「编辑」与「报废」在新界面完成：
  - 新增/编辑走 `POST /api/resources/computer` 与 `PUT /api/resources/computer/{id}`，
    覆盖设备名、组织、设备类型、状态、品牌型号、CPU/内存/存储/显卡、固资编码、SN/ST、
    双 MAC、位置、部门、岗位、购置/注册日期与备注；新增支持「自定义登记 / 从库存登记」
    （从库存登记需选择库存型号与入库仓库）。
  - 报废走 `POST /api/computers/{id}/scrap`，原因取自 `/api/scrap-reasons`，
    提交前二次确认并提示"不回收库存、保留审计"。
- 表头统计（在用/闲置/维修）与表格数据同源，筛选后与表头数字一致。

### 顺带修复

- `/api/state` 的终端对象新增 `position`（岗位）字段：此前该字段只在写入时使用、
  读取时缺失，编辑保存会把岗位清空，属于潜在数据丢失；现已补全。

### 迁移进度

- 已迁移到 Vue：工作台、办公终端、操作日志、设置、设备台账、机柜视图、设备面板、
  网络拓扑共 8 个页面。
- 其余 10 个页面（使用人员、离职人员、IT物资、物资流转记录、报废记录、机房巡检、
  基础字典、工单、服务管理、同步与质量）继续由旧前端在 `/legacy/` 渲染。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建通过；脱敏扫描通过。
- 浏览器端到端：`/computers` 无 iframe；表头各状态数量与筛选结果一致；行首显示设备名、
  组织、设备类型与品牌型号、使用人；编辑弹窗回填设备名与岗位字段；报废弹窗带原因下拉；
  无 JS 报错。
- 回滚：把 `router/index.ts` 的 `computers` 从 `MIGRATED_VIEWS` 移除即回到旧前端渲染。

## v2.18.0

发布日期：2026-09-20

### 变更：工作台改用专用汇总接口

- 新增 `GET /api/workbench/summary`：工作台首屏一次请求拿到全部所需数据，替代原先的
  「`/api/state` + 工单 + 审批 + 变更 + 问题 + 通知 + 巡检 + 数据质量」共 7 个请求。
- 实测体积 **555.9 KB → 4.4 KB（小 127 倍）**，口径与 `/api/state` 完全一致
  （264 台终端 / 239 在用 / 9 闲置 / 16 待关注 / 202 在职 / 56 件非资产 / 1 个根组织）。
- 响应只含：资产计数、最近登记资产 5 条、最近入库 8 条、我的设备，以及按权限裁剪的
  服务队列计数、巡检进度、数据质量 Top 5、通知（未读数 + 最近 5 条）。

### 实现要点

- 复用 `/api/state` 的权限与数据范围过滤（`filter_state_payload`），保证与列表页口径一致；
  组织名与使用人姓名在服务端解析，前端不再拉完整组织/人员列表。
- `build_state_payload(include_audit_logs=False)`：汇总跳过审计日志（整包里最大的一块）。
- 每个模块先判权限（`workbench_can_view`）再取数，无权限模块不出现也不产生 403；
  单模块失败按空值降级（`workbench_collect`），不影响其它模块。
- 汇总按账号做 **15 秒短期缓存**（`WORKBENCH_CACHE_TTL_SECONDS` 可调），`?refresh=1` 绕过缓存；
  缓存键为账号，避免跨账号串数据。前端「刷新数据」按钮即走强制刷新。

### 性能实测（本机 MySQL）

| 请求 | 体积 | 耗时 |
| --- | --- | --- |
| `/api/state`（原方案，另需 6 个请求） | 555.9 KB | 570 ms |
| `/api/workbench/summary` 冷启动 | 4.4 KB | 1.7 s |
| `/api/workbench/summary` 缓存命中 | 4.4 KB | **约 95 ms** |
| `/api/workbench/summary?refresh=1` | 4.4 KB | 1.7 s |

冷启动略慢于 `/api/state`（需额外聚合工单/审批/质量等模块），缓存命中后约为原方案的 1/6；
浏览器实测工作台只发 1 个汇总请求、`/api/state` 调用为 0。生产为 Linux，
查询开销（当前实现按需调用 mysql 客户端）会明显低于本机。

### 验证与回滚

- 单元与结构回归 124 项、`vue-tsc` 类型检查与 Vite 构建、脱敏扫描全部通过。
- 浏览器端到端：9 个模块渲染正常，指标卡与待办数量与接口一致，最近登记资产行显示
  设备名 / 组织 / 设备类型 / 使用人，无 JS 报错。
- 回滚：移除前端对 `/api/workbench/summary` 的调用即可回到多请求方式；接口为只读新增。

## v2.17.0

发布日期：2026-09-20

### 新增：阶段 3 精美度

- **面板图片上传**：设备面板页新增「型号与面板图」对话框，可新建/编辑型号
  （厂商、型号、型号编码、U 高、类别、端口列表每行 `端口名,类型`），并上传正面/背面面板图
  （PNG / JPEG / WebP，单张 2MB 以内）。文件保存到
  `web/assets/device-images/<厂商>/<型号>.<面>.<扩展名>`，面板页把端口热点叠在图片上；
  顶栏可切换「显示面板图」，图片加载失败自动回退到自绘。
  接口 `POST /api/device-types/{id}/image`、`POST /api/device-types/{id}/image/remove`，
  审计动作 `device_type_image_saved` / `device_type_image_removed`。
- **多机柜并排**：机柜视图顶栏新增「单柜 / 并排」切换，并排模式把同一机房的机柜横向排开、
  共用 U 行高，只读展示并支持横向滚动；点任意设备回到单柜视图编辑它。
- **拓扑坐标保存打磨**：拖动节点后顶栏提示「有未保存的拖动」，保存后写入
  `topology_node_position`（v2.11.0 已有能力），未保存时按钮变为「丢弃未保存」。

### 代码精简（马尾辫）

- **标签收敛到单一来源**：机柜设备类型（`RACK_CATEGORY_LABELS` / `rackCategoryLabel`）与
  机房设备状态（`DATACENTER_STATUS_LABELS` / `datacenterStatusTagType`）并入
  `frontend/src/labels.ts`，四个视图里各自维护的标签表删除；
  设备状态显示统一复用既有的 `statusLabel()`。
- **删除机柜上架的两条死路径**：`place_device` 现在只接受机房设备台账（`datacenter_device`），
  移除 `computer`（办公终端）与 `custom`（手工登记）创建分支——待上架列表早已只给台账设备，
  这两条分支属于不再使用的灵活性；历史记录仍可正常读取。该方法由 214 行缩到 136 行。
- 删除无引用常量与函数：`rack_layout.SOURCE_KINDS`、`datacenter_devices.MANUAL_STATUSES`、
  前端 `importDeviceType`（NetBox 导入已移除）与 `updateCable`（界面未使用，接口保留）。

### 数据库与接口

- 不新增数据库迁移；健康检查 `/api/health` 的必需表数量保持 70。
- 新增接口：`POST /api/device-types/{id}/image`、`POST /api/device-types/{id}/image/remove`。

### 验证

- 结构回归：124 项通过（新增 4 项：图片格式识别与存储规则、图片上传路由与前端接线、
  多机柜并排视图、拓扑坐标保存）。
- 前端 `vue-tsc --noEmit` 与 `vite build` 通过。
- 端到端 `qa_device_topology_regression.py` 新增面板图上传/删除（含非图片文件被拒）；
  `qa_rack_layout_regression.py` 改为全程使用机房设备台账（含上架/下架状态回退校验）；
  `qa_datacenter_import_regression.py`、`qa_inspection_regression.py` 同步复跑通过。

### 回滚提示

- 本次不涉及数据库结构变化；面板图片文件位于 `web/assets/device-images/`，
  回退版本后文件仍在，只是界面不再引用。

## v2.16.0

发布日期：2026-09-20

### 变更：资产总览升级为工作台

- 页面定位从"资产总览"改为**工作台**：顶部问候语 + 当日日期 + 待办项数 + 未读通知数，
  并提供「刷新数据」与「自定义工作台」。
- 新增**待办事项**模块：聚合工单 SLA 超时、待处理工单、待我审批、机房巡检待办、
  待关注资产、数据质量待处理、进行中变更与问题记录；只展示有数量的项，SLA 超时与待审批
  标红置顶，点击直接跳转对应页面。
- 新增**快捷入口**模块：按权限提供新建办公终端、创建工单、新增使用人员、物资入库、
  发起机房巡检、设备台账、操作日志、数据质量检查。
- 新增**通知公告**模块：展示当前账号最近通知，未读高亮，可一键标记已读。
- 新增**处理进度**模块：工单状态分布、SLA 超时数、待审批数、巡检完成率（进度条）与
  数据质量问题数。
- 新增**我的设备**模块：按当前账号绑定的人员档案显示名下办公终端（未绑定人员时给出说明）。
- 保留**核心统计**、**最近登记资产**、**最近入库物资**，并把原有的数据质量待处理
  单独成模块。

### 设计要点

- **模块数量控制在 9 个**，待办、快捷入口、核心统计排在最前，保证首屏即见当日工作。
- **基于角色的差异化视图**：模块声明所需权限（如最近登记资产需要 `it_assets`、
  最近入库物资需要 `inventory_catalog`/`warehouse_management`、数据质量需要 `quality`），
  后端接口也只在有权限时才请求，无权限模块不出现、也不产生 403 噪音。
- **自定义设置**：可调整模块顺序与显隐，配置按账号保存在本机浏览器（`oa-workbench-<账号>`），
  并提供「恢复默认」；角色权限是硬约束，自定义只能在其范围内调整。
- 页面数据按模块并行加载，任一接口失败或无权访问只影响该模块（按空值降级），
  不影响其余模块渲染。

### 数据库与兼容

- 不新增数据库迁移与接口：复用 `/api/state`、工单/审批/变更/问题、通知、巡检任务与
  数据质量既有接口；`/api/state` 增加 60 秒会话内缓存，避免来回切换页面重复拉取。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建通过。
- 浏览器端到端：渲染 9 个模块且顺序符合预期；待办 3 项（SLA 超时 1、待关注资产 16、
  数据质量 102）为真实数据；快捷入口 8 个；自定义隐藏「核心统计」后模块数 8，刷新后保持，
  「恢复默认」回到 9；无 JS 报错。
- 回滚：`git revert` 本次提交即可回到 v2.15.0 的总览页；无数据库影响。

## v2.15.0

发布日期：2026-09-20

### 新增

- 资产总览页迁移到 Vue（`/dashboard`）：欢迎区与快捷入口（按权限显示新建资产、创建工单）、
  四张指标卡（办公终端资产与在用率、使用人员与根组织数、闲置可用、待关注与非资产件数）、
  服务工作队列（待处理工单/待审批事项/进行中变更/问题记录，可点击跳转）、
  最近登记资产（按注册日期倒序 Top 5）与最近入库物资（库存增加 Top 8）。
- 新增 `frontend/src/labels.ts`，把状态与设备类型标签（在用/闲置/维修/报废/丢失，笔记本/台式机…）
  收敛为单一来源，操作日志页一并改为复用，避免多处维护。

### 迁移进度

- 已迁移到 Vue：资产总览、操作日志、设置、设备台账、机柜视图、设备面板、网络拓扑共 7 个页面。
- 其余 11 个页面（办公终端、使用人员、离职人员、IT物资、物资流转记录、报废记录、机房巡检、
  基础字典、工单、服务管理、同步与质量）继续由旧前端在 `/legacy/` 中渲染。

### 数据库与兼容

- 不新增数据库迁移与接口：指标复用 `GET /api/state`，服务队列复用既有的工单/审批/变更/问题接口；
  队列接口无权限或不可用时按 0 处理，不影响页面其余部分。

### 验证与回滚

- 单元与结构回归、`vue-tsc` 类型检查与 Vite 构建通过。
- 浏览器端到端：`/dashboard` 已无 iframe；指标卡显示 264 台终端、239 台在用（91%）、202 名在职、
  9 台闲置、16 台待关注、56 件非资产；最近登记资产 5 行；最近入库物资卡片存在；无 JS 报错。
- 回滚：把 `router/index.ts` 的 `MIGRATED_VIEWS.dashboard` 去掉即回到旧前端渲染，不影响数据。

## v2.14.0

发布日期：2026-09-20

### 新增：上传 Excel 导入机房设备

- 机房设备台账（`/datacenter-devices`）新增「导入 Excel」：支持 **.xlsx / .xlsm / .csv**，
  读取第一个工作表，上限 8MB / 1000 行；表头必须包含「设备编号」与「设备名称」，
  其余列按名称自动识别（大小写、空格、下划线、连字符都会被忽略）。
- 可识别列：设备编号、设备名称、品牌型号、设备类型、占用高度、SN/ST、固资编码、使用人、状态、备注。
  设备类型支持中文名（服务器/网络设备/交换机/配线架/UPS/存储…）；占用高度允许写 `2U`、`2`、`2.0`；
  状态只接受「未上架 / 维修 / 报废」（写「上架」会被拒绝，该状态由机柜视图上架操作写入）。
- 先预览后导入：`dryRun` 会返回识别到的列、总行数、将新增/更新/跳过的数量与逐行错误；
  确认后正式导入。重复设备（按设备编号）可选「跳过」或「更新」。
- 逐行容错：单行有问题只影响该行，其余照常导入，结果里给出 Excel 行号与原因。
  已上架设备不能被导入改成「未上架」，必须先到机柜视图下架。
- 台账页提供「下载模板」（带表头与示例行的 CSV）。
- 新增标准库 xlsx 读取器 `office_asset/xlsx.py`（sharedStrings / inlineStr / 数值单元格，
  按单元格引用保留空列；不引入第三方依赖）。

### 变更：移除 NetBox 型号库导入

- 删除 `tools/import_device_types.py` 与 `POST /api/device-types/import`（含服务端的
  NetBox 抓取与 YAML 解析代码）：不再从外部型号库导入型号。
- 型号库改为手工维护：`POST /api/device-types` 提交厂商、型号、U 高、类别与端口列表
  （端口可留空，之后在设备面板逐条补）；审计动作 `device_type_saved`。
  没有型号也能用，设备面板支持逐条新增端口。

### 数据库与接口

- 不新增数据库迁移；健康检查 `/api/health` 的必需表数量保持 70。
- 新增接口 `POST /api/datacenter-devices/import`（权限 `rack_layout:create`），
  请求体 `{"fileName","contentBase64","dryRun","mode"}`。

### 验证

- 结构回归 `python -m unittest tests.test_regressions`：118 项通过（新增 5 项，含 xlsx 读取器与表头映射单测）。
- 前端 `vue-tsc --noEmit` 与 `vite build` 通过。
- 端到端新增 `tests/integration/qa_datacenter_import_regression.py`：预览、正式导入、
  重复设备的跳过/更新两种模式、中文类型与状态转换、逐行错误（类型无法识别/状态写成上架/缺少名称）、
  已上架设备守卫、审计留痕、非表格文件拒绝、只读账号 403，全部通过；
  `qa_device_topology_regression.py`、`qa_rack_layout_regression.py`、`qa_inspection_regression.py`
  同步复跑通过（设备面板的型号改为手工维护后的流程）。

### 回滚提示

- 本次不涉及数据库结构变化；回退代码到 v2.13.0 时新导入的数据仍保留在 `datacenter_device` 中。
- 若要恢复 NetBox 型号库导入，需要同时恢复 `tools/import_device_types.py`、
  `POST /api/device-types/import` 与解析代码。

## v2.13.0

发布日期：2026-09-18

### 新增：机房设备/服务器台账 + 「机房管理」导航分组

- **机房设备台账**：新增 `datacenter_device` 表与「网络设备/服务器」页面
  （`/datacenter-devices`），维护设备编号、名称、型号库关联、品牌型号、类型、U 高、
  SN/ST、固资编码、使用人、状态与备注。办公终端与 IT 物资仍在各自模块管理。
- **未上架设备只来自机房设备**：机柜视图的待上架列表（`GET /api/rack-layout/available`）
  改为只返回状态为「未上架」的机房设备，办公终端（`computer_asset`）与 IT 物资
  （`it_inventory_model`）不再出现在里面。
- **设备状态**：未上架（`stock`）、上架（`installed`）、维修（`repair`）、报废（`scrapped`）。
  「上架」只能由机柜视图的上架操作写入；已上架设备要改回未上架必须先下架；
  设备仍在机柜里时不能在台账里删除；只有未上架设备能上架。上架同步写入所在机房与机柜，
  下架清空位置并回到未上架。
- **「机房管理」导航分组**：把「网络设备/服务器」「机柜视图」「设备面板」「网络拓扑」
  「机房巡检」五个页面收进同一个分组（`frontend/src/navigation.ts` 的 `NAV_GROUPS`）。

### 数据库与接口

- 新增迁移 `database/migrations/20260918_004_datacenter_devices.sql`：新增
  `datacenter_device`；`rack_device_placement` 增加 `datacenter_device_id`（唯一索引）与
  外键，`source_kind` 扩展为 `computer | custom | datacenter`（历史记录继续有效）。
- 新增接口：`GET/POST /api/datacenter-devices`、`PUT /api/datacenter-devices/{id}`、
  `POST /api/datacenter-devices/{id}/remove`；上架接口支持 `sourceKind=datacenter`。
- 健康检查 `/api/health` 的必需表数量由 69 提升为 70。
- 详细说明见 [机房管理](docs/development/datacenter-management.md)。

### 验证

- 结构回归 `python -m unittest tests.test_regressions`：113 项通过（新增 5 项用例）。
- 前端 `vue-tsc --noEmit` 与 `vite build` 通过。
- 端到端 `tests/integration/qa_rack_layout_regression.py` 覆盖新流程：新建机房设备 →
  未上架列表只出现机房设备（断言办公终端不出现）→ 上架后状态为 `installed` 且写入机柜 →
  下架后回到 `stock` 并清空位置；`qa_device_topology_regression.py`、
  `qa_inspection_regression.py`、`qa_scrap_regression.py` 同步复跑通过。

### 回滚提示

- 本次只新增表与列；回退代码到 v2.12.0 时 `datacenter_device` 与
  `rack_device_placement.datacenter_device_id` 可保留（旧版本不读取），彻底清理请先备份。
- `server.py` 的健康检查门槛与表清单必须与数据库同步回退，否则 `/api/health` 返回 503。

## v2.12.0

发布日期：2026-09-18

### 新增

- 设置页新增「外观主题」：8 种预设主题色、自定义取色器与「恢复默认」，实时生效并保存在本机浏览器。
  主题色写入 Element Plus 的 `--el-color-primary` 及其 light/dark 派生变量，顶栏、侧栏与所有 Vue
  页面同步；迁移期还会把旧前端 iframe 的强调色（`--teal` / `--teal-soft`）一并跟随。
- 操作日志页迁移到 Vue（`/audit`）：支持日期范围、人员、关键词、操作类别、具体变动、对象范围筛选，
  分页表格、详情抽屉与 CSV 导出（带 BOM，Excel 可直接打开）；操作类别、具体变动、变更前后等
  标签映射与旧前端完全一致。

### 迁移进度

- 已迁移到 Vue：设置、操作日志、机柜视图、设备面板、网络拓扑。
- 其余 12 个页面继续由旧前端在 `/legacy/` 中渲染，导航与顶栏已统一由新外壳接管。

### 数据库与兼容

- 不新增数据库迁移与接口；操作日志沿用既有的 `GET /api/audit-logs`。
- 主题色只影响本机浏览器显示，不写入服务端配置。

### 验证与回滚

- 单元与结构回归、安全集成回归、`vue-tsc` 类型检查与 Vite 构建通过。
- 浏览器端到端：操作日志显示 1230 条真实记录、关键词筛选后 356 条；主题色切换为「标准红」后
  `--el-color-primary` 与派生色同步变化、刷新后保持、旧前端强调色跟随，恢复默认后回到 `#409eff`。
- 回滚：删除 `frontend/src/views/AuditView.vue` 并把路由注册表里的 `audit` 改回旧前端即可；
  主题色是纯前端特性，删除后不影响数据。

## v2.11.0

发布日期：2026-09-18

### 新增：设备面板与网络拓扑（阶段 1 + 阶段 2）

- **型号库**：新增 `device_type_catalog` 与 `device_type_port_template`，从 NetBox
  devicetype-library（CC0）导入厂商型号、U 高、类别、面板图与端口模板；解析器只覆盖该库的
  YAML 子集（顶层标量 + `interfaces`/`power-ports`/`console-ports`），不引入第三方依赖。
- **端口**：`rack_device_port` 属于已上架设备（`rack_device_placement`），支持端口名、类型、
  类别、面板、行列位置、方向、速率与状态；按型号模板生成端口是快照，之后可单独增删改。
- **线缆**：`rack_cable_run` 记录 A/B 两端端口、介质、长度、标签与状态；
  **一个端口只能有一条活动链路**（生成列 + 唯一索引），不允许自己连自己，跨机柜与跨站点允许。
- **设备面板页**（`/device-panel`）：按型号渲染设备面板，有面板图时用图片叠加端口热点，
  没有时按端口模板自绘；端口按已连接/空闲/断开着色，点两个端口即可建立链路，
  支持新增/编辑/删除端口、按型号一键生成端口、查看与删除本机柜链路。
- **网络拓扑页**（`/topology`）：按端口连接自动分层（边界/核心 → 汇聚 → 接入），
  节点可拖动微调并保存坐标，支持按机房/机柜过滤、只看有链路的设备、链路标签开关，
  以及导出 SVG、导出 PNG（SVG → Canvas）与打印。
- **机柜视图迁移到 Vue**：`/rack-layout` 改由 `frontend/src/views/RackLayoutView.vue` 渲染，
  旧前端的同名实现已删除；功能保持不变（拖动换 U 位、上架下架、属性编辑、导出清单、
  打印机柜图、发起巡检）。
- 型号导入工具 `tools/import_device_types.py`：支持 `netbox`（GitHub 型号库，含面板图）、
  `file`、`dir` 三种来源，`SOURCES` 注册表可扩展，支持 `--list`、`--dry-run`、`--no-images`。

### 数据库与接口

- 新增迁移 `database/migrations/20260918_003_device_ports_and_cables.sql`：新增
  `device_type_catalog`、`device_type_port_template`、`rack_device_port`、`rack_cable_run`、
  `topology_node_position` 五张表，只新增对象，不改动历史数据。
- 新增接口：型号库列表/详情/导入，机柜端口清单，端口增删改与按模板生成，
  链路增删改与批量导入，拓扑数据与坐标保存；权限沿用 `rack_layout` 模块。
- 健康检查 `/api/health` 的必需表数量由 64 提升为 69。
- 详细说明见 [设备面板与网络拓扑](docs/development/device-panel-and-topology.md)。

### 验证

- 结构回归 `python -m unittest tests.test_regressions`：106 项通过（新增 7 项用例，含 YAML 解析器单测）。
- 前端 `vue-tsc --noEmit` 类型检查与 `vite build` 生产构建通过。
- 端到端回归 `tests/integration/qa_device_topology_regression.py`：型号导入、端口模板生成（幂等）、
  手工端口、机柜端口清单、链路创建与幂等、端口占用冲突、拓扑节点与链路、坐标保存、
  删除链路与端口、审计留痕、只读账号 403，全部通过。
- 机柜视图、机房巡检的端到端回归同步复跑通过。

### 回滚提示

- 本次只新增表与接口；回退代码到 v2.10.0 时这五张表可保留（旧版本不读取），
  如需彻底清理请先备份再删除。
- `server.py` 的健康检查门槛与表清单必须与数据库同步回退，否则 `/api/health` 返回 503。
- 机柜视图已从旧前端移除，旧前端 `web/app.js` 不再包含该页面；如需回退到旧实现，
  必须同时回退 `frontend/` 与 `web/` 两侧代码。

## v2.10.0

发布日期：2026-09-18

### 新增

- 前端迁移到主流 SPA 技术栈：Vue 3 + TypeScript + Vite + Element Plus + Vue Router，
  源码位于 `frontend/`，构建产物输出到 `web/app/`。
- 引入真实 URL 路由（History API）：`/dashboard`、`/computers`、`/rack-layout`、
  `/settings` 等，支持刷新保持当前页、收藏与分享链接、浏览器前进后退。
- 设置页新增「系统信息」，可以看到**当前系统版本**、前端构建版本与构建时间、服务端时间与时区、
  数据库连接状态；登录页与顶栏也会显示当前版本。
- 版本号改为单一来源：仓库根目录 `VERSION`，后端通过新增的 `GET /api/meta` 暴露，前端构建时注入。
- 顶栏统一承载消息提醒、主题切换、重新加载数据、设置入口与退出登录。

### 迁移方式（渐进）

- 旧前端完整保留在 `web/` 并由 `/legacy/` 提供，新外壳用同源 iframe 承载尚未迁移的页面，
  并代理其主题与页面切换；除设置页外其余 14 个页面暂由旧前端渲染。
- 为保证同源 iframe 可用，仅 `/legacy/*` 的响应放宽为 `X-Frame-Options: SAMEORIGIN` 与
  `frame-ancestors 'self'`，其余响应仍为 `DENY` / `frame-ancestors 'none'`。
- 旧前端新增 `window.oaLegacy` 桥接接口（读取/切换页面、主题、登录态），逐页迁移完成后可整体删除。

### 构建与部署

- `Dockerfile` 改为多阶段构建：`node:22-alpine` 阶段执行 `pnpm install --frozen-lockfile && pnpm build`，
  运行阶段只复制构建产物与 `VERSION`。
- 本地开发：`cd frontend && pnpm install && pnpm build`（或 `pnpm dev` 起开发服务器）；
  未构建时后端自动回落旧版入口，功能不受影响。
- CI 增加前端依赖安装、类型检查与构建步骤。

### 数据库与兼容

- 不新增数据库迁移，接口保持不变；仅新增公开的 `GET /api/meta`（只返回版本与时间，不含主机与凭据信息）。
- 浏览器需要支持 ES2020 与 History API。

### 验证与回滚

- 单元与结构回归 97 项通过；`vue-tsc` 类型检查与 Vite 生产构建通过。
- 用真实浏览器端到端验证：登录后跳转 `/dashboard` 并稳定停留；侧边导航切换 `/computers`、
  `/inventory`、`/inspection` 与旧前端页面标题一致；设置页显示 `v2.10.0`。
- 回滚：删除 `web/app/`（后端会自动回落旧版入口）即恢复旧界面，数据库与接口无需回滚。

## v2.9.0

发布日期：2026-09-18

### 新增：机柜视图（独立模块）

- 新增独立模块「机柜视图」，权限模块 `rack_layout`，与「机房巡检」共用机房与机柜对象，
  但职责分开：巡检管检查事项，机柜视图管设备装在哪。
- 把台账设备放到机柜的具体 U 位：起始 U 位、占用高度（整数 U，1-50）、
  安装面（前面板 / 后面板 / 整机深度），并可按 12 类机柜设备类型分类着色。
- 位置规则：前后面板各自成层、可以同位共存；整机深度独占该 U 位；
  越界、重叠、同一台办公终端重复上架都会被拒绝；已报废归档的设备不能上架。
- 占用统计按物理 U 行去重：前后面板重叠不会重复计数。
- 上架与下架都不改变库存和资产状态；固资编码、SN、使用人、状态仍以资产台账为准，
  机柜位置变化写入审计。
- 前端：侧栏「机柜视图」支持选中改属性、拖动换 U 位（吸附整数 U、冲突红框拒绝）、
  从右侧未上架设备池点选后点空位上架、前后面板切换、导出设备清单（Excel）、
  打印机柜图（逐 U 打印视图），以及按当前机柜一键「发起巡检」。

### 数据库与接口

- 新增迁移 `database/migrations/20260918_002_rack_layout.sql`：新增
  `rack_device_placement` 表与 `rack_layout` 权限模块，只新增对象，不改动历史业务数据。
- 新增接口：`GET /api/rack-layout/racks`、`GET /api/rack-layout/racks/{id}`、
  `GET /api/rack-layout/available`、`POST /api/rack-layout/racks/{id}/placements`（支持
  `Idempotency-Key`）、`PUT /api/rack-layout/placements/{id}`、
  `POST /api/rack-layout/placements/{id}/remove`。
- 审计动作：`rack_placement_created`、`rack_placement_updated`、`rack_placement_removed`。
- 健康检查 `/api/health` 的必需表数量由 63 提升为 64。
- 详细说明见 [机柜视图](docs/development/rack-layout.md)。

### 验证

- 结构回归 `python -m unittest tests.test_regressions`：92 项通过（新增 5 项机柜视图用例）。
- 端到端回归 `tests/integration/qa_rack_layout_regression.py`：机柜列表占用统计、
  未上架设备来源、上架与幂等重复提交、同面重叠拒绝、前后面板同位共存、越界拒绝、
  移动与改高度的二次校验、下架后重新出现在未上架列表、审计留痕、只读账号被拒绝，全部通过。
- 机房巡检端到端回归 `tests/integration/qa_inspection_regression.py` 同步复跑通过。

### 回滚提示

- 本次只新增表、权限与接口，不改动既有表结构；回退代码到 v2.8.0 时，
  `rack_device_placement` 可以保留（旧版本不读取），如需彻底清理请先备份再删除该表。
- `server.py` 的健康检查门槛与表清单必须和数据库同步回退，否则 `/api/health` 会返回 503。

## v2.8.0

发布日期：2026-09-18

### 新增：机房巡检管理

- 巡检对象只包含**机房、弱电间和它们下面的机柜**，不含办公区终端与 IT 物资仓库。
- 新增巡检模板与事项字典：类别、事项、检查方法、取值类型（正常异常 / 数值 / 文本 / 选项）、
  单位、正常范围、是否必填。迁移自带 `XJ-SERVER-ROOM`（机房巡检，16 项）与
  `XJ-WEAK-ROOM`（弱电间巡检，14 项）两张标准模板。
- 开始巡检时把模板事项**快照**进本次任务明细，之后修改模板不影响历史巡检表。
- 执行页逐项输出巡检事项：正常 / 异常 / 不适用三键记录，可填实测值与说明；
  异常项必须填写说明，填写与提交两个环节都会校验。
- 提交即定稿：未完成全部事项不能提交，提交后任务与明细只读，不能继续修改或作废；
  作废未提交的任务必须填写原因。
- 巡检表输出：整页预览、导出 Excel（巡检表摘要 + 巡检明细两个页签）、打印视图（带巡检人 /
  复核人签字栏）。
- 执行人默认为发起巡检的账号；不生成周期计划、不自动派单，只能手动点击“开始巡检”。
- 扫码开检：打开 `/#inspection?rack=<机柜编码>` 会自动进入巡检页并预选该机柜，机柜编码不存在时不弹窗。

### 权限与审计

- 新增权限模块 `inspection_management`（查看 / 创建 / 修改 / 删除 / 导出），
  管理员角色默认获得查看、创建、修改与导出。
- 审计动作：`inspection_site_created`、`inspection_site_updated`、`inspection_rack_created`、
  `inspection_rack_updated`、`inspection_template_created`、`inspection_template_updated`、
  `inspection_started`、`inspection_submitted`、`inspection_voided`。

### 数据库与接口

- 新增迁移 `database/migrations/20260918_001_inspection_management.sql`：新增
  `asset_site`、`asset_rack`、`inspection_template`、`inspection_template_item`、
  `inspection_task`、`inspection_task_item` 六张表，写入两张标准模板与权限数据，
  只新增对象，不改动历史业务数据。
- 新增巡检接口：机房/弱电间、机柜、模板的资源接口，以及
  `POST /api/inspection/tasks`（支持 `Idempotency-Key`）、
  `POST /api/inspection/tasks/{id}/items/{itemId}/check`、
  `POST /api/inspection/tasks/{id}/submit`、`POST /api/inspection/tasks/{id}/void`。
- 健康检查 `/api/health` 的必需表数量由 57 提升为 63。
- 详细说明见 [机房巡检管理](docs/development/inspection-management.md)。

### 验证

- 结构回归 `python -m unittest tests.test_regressions`：87 项通过（新增 6 项巡检用例）。
- 端到端回归 `tests/integration/qa_inspection_regression.py`：模板读取、机房与机柜创建、
  开始巡检快照、幂等重复提交、异常项必须填说明、未完成不可提交、提交后只读、
  模板与对象不匹配被拒绝、审计留痕、只读账号被拒绝，全部通过。

### 回滚提示

- 本次只新增表、权限与接口，不修改既有表结构；回退代码到 v2.7.3 时，巡检相关表可以保留
  （旧版本不读取这些表），如需彻底清理请先备份再删除六张巡检表。
- `server.py` 的健康检查门槛与表清单必须和数据库同步回退，否则 `/api/health` 会返回 503。

## v2.7.3

发布日期：2026-09-17

### 修复

- 全栈时区由 UTC 切换为 `Asia/Shanghai`（UTC+8）：此前宿主机、应用容器、MySQL 均为
  UTC，而库中时间列全是 `DATETIME`（无 `TIMESTAMP`），MySQL 不做时区换算、前端也只按
  字符串裁剪，用户看到的时间比北京时间少 8 小时。
- `compose.yaml` 的 `db`、`migrate`、`app` 三个服务新增 `TZ: ${TZ:-Asia/Shanghai}`，
  容器时区可再用环境变量覆盖。
- 新增一次性数据迁移脚本 `database/manual/20260917_001_timezone_utc_to_asia_shanghai.sql`：
  按表整体把历史 `DATETIME` 值后移 8 小时，带执行标记表保证幂等，重复执行会因主键冲突中止。
- 新增 [时区迁移说明](docs/development/timezone-migration.md)，记录迁移范围判定方法、
  执行步骤、回滚方式，以及在演练中发现的两个陷阱：
  逐列平移会违反行内 `CHECK` 约束；条件平移会触发 `ON UPDATE CURRENT_TIMESTAMP`
  把 `updated_at` 刷成当前时间（脚本中已用 `updated_at = updated_at` 规避）。
- 例外处理：`inventory_movement_log.occurred_at` 与 `left_employee_archive.archived_at`
  存在浏览器按本地时间写入的历史值，只平移与同行 `created_at` 一致的记录；
  `itil_change.planned_start_at/planned_end_at` 由用户手填，不平移。

### 数据库与兼容

- 不新增结构迁移；时区数据迁移是手工脚本，不进入自动迁移流程，需人工确认后执行。
- 迁移后所有业务时间列均为北京时间，历史记录时间与实际发生时间一致。
- 宿主机、Gitea、1Panel 组件不在本仓库范围，需按文档单独设置时区；
  Gitea 使用 PostgreSQL `timestamptz`，切换时区不需要迁移数据。

### 验证与回滚

- 流程：全库备份 → 还原出「迁移前/迁移后」两个库做逐表逐列比对 → 生产切换。
  演练结果：60 张数据表行数完全一致，58 张零差异，例外列 186/31 与 10/5 符合预期。
- 生产校验：`audit_log` 最早记录 10:57 → 18:57、最新 07:50 → 15:50；
  无未来时间行、`updated_at` 未被污染、既有会话仍然有效。
- 回滚：删除标记行后用 `INTERVAL -8 HOUR` 重跑，或恢复迁移前备份并把 `TZ` 改回 UTC。

## v2.7.2

发布日期：2026-09-17

### 加固与修复（无功能变更）

- `deploy/nginx/office-asset-mgmt.conf` 补齐代理层配置：`server_tokens off`、
  `more_clear_headers Server`，以及与后端一致的一组安全响应头
  （CSP、X-Content-Type-Options、X-Frame-Options、X-XSS-Protection、Referrer-Policy、
  Permissions-Policy、COOP、CORP），并保留没有 headers-more 模块时的替代写法。
- 新增 [扫描整改说明](docs/security/tscanplus-remediation.md)：复核外部扫描命中的其实是
  OpenResty 静态占位站点，给出端口仅本机访问的操作步骤、代理层加固步骤和 CSRF 复测证据；
  结论是该条 CSRF 为误报。
- 安全集成回归新增 `csrf_scanner_referer` 检查：按扫描器方式伪造 `Referer`/`Origin`，
  验证缺少令牌与令牌错误都返回 `403`，带正确令牌的写请求正常受理。
- 修复安全集成回归自 v2.6.0 起的失败用例：物资回收必须显式指定目标仓库后，
  `POST /api/inventory/allocations/<id>/return` 的用例缺少 `warehouseId`，现已补上。

### 数据库与兼容

- 不新增数据库迁移，接口与业务规则不变，后端代码未改动。

### 验证与回滚

- 安全集成回归全部通过，输出包含 `QA_REGRESSION_PASS` 与 `csrf_scanner_referer`。
- 代理层配置已在生产实际使用的 OpenResty 版本上完成 `nginx -t` 语法校验。
- 回滚到 v2.7.1 不需要数据库操作；回滚后代理层响应头需要单独恢复。

## v2.7.1

发布日期：2026-09-16

### 优化（无功能变更）

- 删除前端 18 个已废弃函数（约 450 行），包括旧版物资页渲染、旧人员表格、旧仪表盘组织树、旧导出逻辑，以及已由后端离职命令接管的前端归档函数。
- 删除 15 个无引用 CSS 类及其规则（约 139 行），例如 `content-grid`、`dashboard-status-*`、`number-grid`、`inline-form`、`monitor-row`。
- 删除后端死代码约 360 行：`server.py` 的 `build_inventory_audit_entries`（已被 `_v2` 取代）与 `sync_state`、`operations.py` 三个未使用的 `_apply_*`、`service_management.ticket_extension_row`、`asset_service._assert_inventory_global_operation`。
- 离职回收入库 SQL 由“按分配逐批次 + 兜底”两条路径合并为单一路径（减少约 150 行），统一按解析（必要时自动新建品牌型号）后的库存型号入库。
- 模态框改用原生 `<dialog>` + `showModal()`，获得 ESC 关闭、焦点陷阱和顶层渲染，样式由 `dialog.modal-dialog` 承担。

### 数据库与兼容

- 不新增数据库迁移，接口与业务规则不变。
- 前端静态资源版本更新为 `20260916-v2.7.1-r0`。

### 验证与回滚

- 单元与结构回归 81 项、离职集成回归、报废集成回归全部通过；发布前脱敏扫描通过。
- 回滚到 v2.7.0 不需要数据库操作；静态资源版本号会随之变化，浏览器需刷新一次。

## v2.7.0

发布日期：2026-09-16

### 新增

- 使用人员新增“公用”状态（`shared`）：用于给非个人使用的设备一个可挂靠的虚拟使用人，与“在职/停用/离职”并列。
- 一个组织下可以建立多个公用人员，名称自由填写（例如“<组织名>公用”）。
- 公用人员工号自动按 `SHARED-<组织编码>-NN` 生成，同时允许手工指定工号。
- 公用人员可以像普通员工一样被分配办公终端、领用显示屏和非资产物资；设备分配下拉与“离职资产转交他人”的接收人候选都包含公用人员。
- 人员列表默认显示公用人员并带“公用”状态标签，支持按状态筛选。
- 组织树的人员数字新增“组织树人数含公用人员”开关，默认排除公用人员，可随时切换。

### 约束与处理规则

- 公用人员禁止绑定登录账号：后端校验会拒绝绑定，前端账号绑定下拉也不再列出公用人员。
- 公用人员的“办理离职”按钮显示为“删除”，点击后按删除处理：释放名下资产与物资、停用人员记录，**不写入离职人员档案**，因此不会出现在离职人员页面。
- 公用人员可以停用或改回在职；已离职人员不能直接改成公用。
- 数据库 `ck_employee_status` 放宽为允许 `shared`（新迁移 `20260916_001_shared_employee_status.sql`，仅调整约束，不改动历史数据）。

### 数据库与兼容

- 新增迁移 `20260916_001_shared_employee_status.sql`：重建员工状态检查约束以允许 `shared`。
- 不需要其它结构调整；公用人员沿用现有的人员、设备分配、领用与离职命令。

### 验证与回滚

- 单元与结构回归 81 项通过。
- 离职集成回归新增公用人员场景：自动生成 `SHARED-` 工号、可挂靠办公终端、禁止绑定登录账号、删除后不进入离职人员档案。
- 回滚到 v2.6.0 前应先确认没有 `shared` 状态的人员记录，否则旧约束会拒绝删除该状态；如需回滚先将其改为在职或停用。

## v2.6.0

发布日期：2026-09-15

### 新增

- 回收统一入库：只要是登记在人员名下的物资，回收时一律进入库存并输出流转记录，不再区分当初是否扣减过库存。
- 缺少品牌型号自动建档：回收时如果物资没有对应库存型号，系统按“物资类型 + 品牌 + 型号”自动创建品牌和型号（型号名缺失时记为“未填写型号”，品牌缺失时记为“未填写品牌”），再按所选仓库入库；已有同品牌同型号时直接复用，不重复建档。
- 回收完成弹窗：离职办理或设备清单回收完成后弹出“回收完成”对话框，逐条列出已回收物资（类型、品牌型号、数量、入库仓库、流转记录编号）；未回收的项目在同一弹窗中说明原因（转交他人、异常待处理或未选择回收）。

### 修复

- 修复“仅登记、不扣减库存”的物资离职后既不在人员名下也不在库存里的问题：现在会按回收仓库补登库存并写入离职回收流转记录。
- 单个物资回收（设备清单中的“回收选中”、按领用记录归还）同样改为强制选择回收仓库并入库，缺少型号时自动补齐目录。
- 离职接口返回新增 `recoveryRecords`（本次生成的回收记录）与 `unrecoveredItems`（未回收项目及原因），供前端弹窗展示。

### 数据库与兼容

- 不新增数据库迁移；自动创建的品牌/型号写入 `it_inventory_brand`、`it_inventory_model`，入库写入 `inventory_warehouse_stock` 与 `inventory_movement_log`。
- 回收仓库仍然必填，缺少目标仓库时接口返回 `400`。
- 回收产生的库存与流转记录可以通过“物资流转记录”页面按 `leave_recovery` / `inventory_return` 触发动作查询。

### 验证与回滚

- 新增结构回归覆盖自动建档、回收入库与弹窗展示；单元与结构回归 77 项通过。
- 离职集成回归覆盖“缺仓库被拒绝”“已扣库存物资回到指定仓库”“登记物资自动建档并入库”“归档快照留痕”，报废集成回归继续通过。
- 回滚代码不会撤销已经写入的库存、流转和档案记录；如需回滚数据应使用升级前备份。

## v2.5.0

发布日期：2026-09-15

### 新增

- 版本更新面板增加“更新来源”选择：默认使用内置的 GitHub 官方仓库地址，检查按钮显示为“从 GitHub 检查更新”，无需再手工填写项目地址。
- 新增“自定义更新地址”来源：切换到该选项后可以填写其它 GitHub/Gitea 仓库地址，留空时回退到服务器部署目录的默认 origin。
- 两种来源可以随时切换，切换后会清空上一次的检查结果，必须重新检查后才能执行更新，避免用旧结果跨来源更新。

### 修复

- 内置 GitHub 来源只用于本次检查与更新，不再写入 `update_repository_url` 设置，避免覆盖服务器上按环境配置的默认 origin；只有“自定义更新地址”才会保存该设置。

### 数据库与兼容

- 不新增数据库迁移。
- 接口 `/api/updates/check` 新增可选字段 `persistRepositoryUrl`（默认 `true`，保持旧客户端行为）；`false` 时仅用于本次请求，不更新设置。

### 验证与回滚

- 新增结构回归覆盖内置来源常量、来源切换、检查按钮文案与“仅自定义来源持久化地址”。
- 单元与结构回归 74 项、报废与离职集成回归、发布前脱敏扫描均通过。
- 回滚到 v2.4.1 不需要调整数据库；已保存的自定义地址仍然保留在设置中。

## v2.4.1

发布日期：2026-09-15

### 修复

- 更新 `web/index.html` 静态资源缓存版本为 `20260915-v2.4.1-r0`，避免浏览器继续使用 v2.3.0 的 `app.js`，导致离职回收强制选择仓库的新规则不生效。

### 数据库与兼容

- 不新增数据库迁移，不含数据结构变更。
- 仅前端静态资源版本变化，后端接口与 v2.4.0 一致。

### 验证与回滚

- 部署后确认页面加载的 `app.js?v=20260915-v2.4.1-r0` 包含“选择回收时必须指定回收目标仓库”规则。
- 回滚到 v2.4.0 不需要调整数据库，但用户浏览器可能继续使用旧缓存，需要强制刷新。

## v2.4.0

发布日期：2026-09-15

### 新增

- 离职回收必须选择回收目标仓库：办理离职时，每一项选择“回收”的资产或物资都必须明确指定回收到哪个仓库。
- 离职表单不再预选默认仓库，未选择回收仓库时前端阻止提交、后端返回 `400`。
- 回收仓库对所有回收项生效：扣减过库存的物资按所选仓库回补库存；仅登记未扣减库存的物资不改动库存，但同样记录回收仓库，便于实物追溯。

### 修复

- 修复离职回收仓库参数不匹配的问题：后端此前只读取 `warehouseId`，而离职表单提交的是 `recoveryWarehouseId`，导致已扣减库存的物资在离职回收时无法指定目标仓库。
- 离职档案快照现在记录每一项的回收目标仓库与仓库名称，审计和后续核查可以直接看到实物去向。

### 数据库与兼容

- 不新增数据库迁移，复用 `inventory_allocation_history.warehouse_id` 和离职档案快照字段。
- 前端与后端需同时升级：新前端始终提交 `recoveryWarehouseId`，新后端对缺少仓库的回收项返回 `400` 并提示“选择回收时必须指定回收目标仓库”。

### 验证与回滚

- 新增集成回归 `tests/integration/qa_offboarding_regression.py`，覆盖缺少仓库被拒绝、库存回到指定仓库、未扣库存物资记录仓库、归档快照留痕。
- 单元与结构回归、报废集成回归、脱敏扫描均通过。
- 回滚代码不会撤销已经生成的离职档案与库存回补记录；如需回滚数据应使用升级前备份。

## v2.3.0

发布日期：2026-09-15

### 新增

- 新增“发放中的 IT 物资直接报废”：显示屏、鼠标、键盘等已领用物资可直接选择报废，不进入回收仓库、不回补库存。
- 新增“办公终端报废”：设备报废后退出在用台账并软归档，资产主记录、领用历史、状态历史和报废快照全部保留。
- 新增报废记录查询页，可查看/导出报废时间、物品信息、使用人、报废原因、操作人、备注和附件预留字段。
- 新增报废原因字典 `scrap_reason`，内置损坏无法修复、老化到寿命期限、人为损坏、外借丢失、技术淘汰和其他原因。

### 接口

- `POST /api/inventory/usage/{type}/{id}/scrap`：按人员物资使用记录报废，需要 `scrap_management` 报废权限。
- `POST /api/computers/{id}/scrap`：办公终端报废并软归档。
- `GET /api/scrap-records`、`GET /api/scrap-records/{kind}:{id}`、`GET /api/scrap-reasons`：查询报废记录、明细和原因字典。

### 修复与约束

- 已扣减库存的物资报废后不返还库存；仅登记未扣库存的物资报废后不产生任何库存变化。
- 报废不会增加 `it_inventory_model.quantity` 或 `inventory_warehouse_stock.quantity`，也不会写入虚假的物资流转入库记录。
- 使用记录不会被删除：报废后使用记录保留并标记为停用，原领用记录状态改为 `scrapped`，与“已归还”区分。
- 办公终端报废后不再出现在台账和 `/api/state` 中，但仍可通过报废记录追溯完整配置、固资编码、SN/ST 和使用人。
- 报废命令带幂等键，重复提交返回同一结果；重复报废已归档设备返回冲突而不是二次写入。

### 数据库与兼容

- 新增迁移 `20260915_001_scrap_management.sql`，新增 `scrap_reason`、`inventory_scrap_record`、`asset_scrap_record` 三张表，并为 `computer_asset` 增加 `is_archived`、`archived_at`、`archived_by` 列。
- `inventory_allocation_history.status` 增加 `scrapped` 状态。
- 新增权限模块 `scrap_management`（报废管理），默认授予管理员查看、创建和导出权限。
- 不修改已发布迁移，不回写历史业务数据；旧库升级前必须备份。
- `/api/health` 必需表数量从 54 提升到 57，升级后未执行迁移会返回 `503`。

### 验证与回滚

- 新增接口集成回归 `tests/integration/qa_scrap_regression.py`，覆盖发放物资报废、仅登记物资报废、办公终端报废归档、台账隐藏、幂等和权限拒绝。
- 单元与结构回归 `python -m unittest discover -s tests` 通过。
- 回滚代码不会撤销已经写入的报废记录、归档标记和审计日志；如需回滚业务数据，应停止写入并使用升级前备份恢复。

## v2.2.3

发布日期：2026-09-14

### 修复

- 修复离职办理时显示屏和非资产物资可能未回收入库、随后从人员名下直接消失的问题。
- 离职办理现在要求提交清单完整覆盖当前名下的电脑、显示屏和非资产物资；清单发生变化或缺项时事务整体拒绝并回滚。
- 对已扣减库存的物资按实际库存型号、数量和回收仓库分别回补库存并写入离职回收流水；仅登记未扣库存的物资不会虚增库存。

### 数据库与兼容

- 本版本不新增数据库迁移，不修改已发布迁移或历史数据。
- 升级前仍需备份数据库；回退代码不会回退已经产生的业务流水。

### 验证与回滚

- 已增加离职清单完整性回归检查，并运行完整单元回归。
- 如需回滚，仅回退应用代码到上一稳定版本；已产生的回收入库记录应通过业务回滚或数据库备份恢复处理。

## v2.2.2

发布日期：2026-09-09

### 修复

- 修复同一人员无法分别领取两条品牌、型号和库存型号均相同的非资产物资的问题。
- 每次库存领用创建独立的人员物资使用记录和库存分配历史，支持逐条显示、转交、回收和审计。
- 离职回收和单条回收只处理对应的使用记录，不会因同型号记录合并而重复回收库存。
- `/api/state` 兼容写入允许同一人员保存多条相同品牌型号的物资记录。

### 数据库与兼容

- 新增 `20260909_001_individual_inventory_usage_records.sql`，将人员物资表上的同型号唯一索引改为普通索引。
- 不修改已发布迁移；历史已经合并的使用记录不回写，仍按原数量和关联历史兼容回收。

### 验证与回滚

- 已增加同一人员同一库存型号连续两次领用、分别回收的集成回归覆盖。
- 升级前必须备份数据库，并先在隔离库执行迁移和回归测试。
- 代码回退不会自动恢复已删除的唯一索引；如需恢复旧结构，应停止业务并从升级前备份恢复。

## v2.2.1

发布日期：2026-09-08

### 修复

- 修复生产服务器升级到 v2.2.0 时，MySQL 8.4 在人员物资表添加 `STORED`
  生成列返回 `ERROR 1215`，部署失败后自动回滚到 v2.0.17 的问题。
- 兼容迁移使用 `VIRTUAL` 生成列保留按 `inventory_model_id` 区分同型号不同库存批次的规则。
- 保留离职回收、同型号不同品牌/购买批次分开记录以及自定义物资不调整库存的现有行为。

### 数据库与兼容

- 新增按顺序先于历史迁移执行的
  `20260907_000_usage_inventory_model_identity_compatibility.sql`。
- 不修改已发布的 `20260907_001_usage_inventory_model_identity.sql`，避免生产迁移校验和变化。
- 空库初始化结构同步使用 `VIRTUAL` 生成列；已有数据、外键和历史索引均保留并安全切换。

### 验证与回滚

- 升级前必须执行生产数据库备份，并确认 `schema_migration` 中已有迁移记录完整。
- 先在 MySQL 8.0/8.4 隔离库验证迁移，再通过受控更新脚本部署；迁移失败仍会自动恢复旧应用提交。
- 代码回退不会撤销已执行迁移；如需恢复旧结构，应停止业务并从升级前备份恢复。

## v2.2.0

发布日期：2026-09-07

### 新增与修复

- 物资分配按 `inventory_model_id` 区分不同购买时间或库存批次的同品牌同型号物资。
- 同一人员可以同时持有不同库存批次的同型号物资，转交时也不会错误合并。
- 离职回收按活动分配记录实际关联的库存型号和数量分别回收入库。
- 未扣减库存的登记记录在离职时只归档并关闭使用记录，不会增加库存。
- 增加 `tools/next_version.py`，按 Conventional Commit 自动生成下一版 SemVer。

### 数据库与兼容

- 新增增量迁移 `20260907_001_usage_inventory_model_identity.sql`。
- 历史人员物资记录和流转记录保留；自定义物资仍按完全相同的品牌型号合并。

### 验证与回滚

- 升级前备份数据库，执行迁移后运行完整回归测试和库存回收核对。
- 代码回退不会撤销已执行迁移；如需恢复旧结构，应先停止业务并从升级前备份恢复。

## v2.1.0

发布日期：2026-09-07

### 新增

- 办公终端新增“自定义品牌型号”和“从仓库库存登记”两种登记方式。
- 自定义登记不读取库存、不扣减库存，也不强制关联库存型号。
- 仓库登记只展示所选授权仓库中数量大于零的电脑库存，并自动带出品牌、型号、配置和购置日期。
- 仓库登记允许修改设备资料，但库存扣减仍以选定库存型号为准；库存不足时事务不会创建设备。
- 同型号不同购置日期的电脑库存批次在选择项中分别显示批次信息。
- 物资分配、设备回收和离职回收按操作对象或操作人的组织层级选择最近的启用仓库，
  同组织按仓库名称排序；仍可手动选择其他有权限访问的启用仓库。
- 分配后设备不再绑定仓库，但分配和回收流转记录保留仓库名称。

### 修复

- 修复办公终端登记表单仍调用旧库存选择监听器的问题。
- 修复自定义登记模式下隐藏的仓库必填字段阻止表单保存的问题。
- 修复编辑办公终端时可能重复调整库存的问题；编辑只更新设备资料。

### 数据库与兼容

- 本版本不新增数据库迁移，不修改已有历史流转记录。
- 保留现有 `GET /api/state` 兼容读取接口及旧客户端的自定义登记兼容行为。

### 验证与回滚

- 已补充组织范围默认仓库、登记模式、库存批次显示和库存不足保护的结构回归测试。
- 回滚只需回退应用代码至 `v2.0.17`；本版本没有新增需要回滚的数据库迁移。

## v2.0.17

发布日期：2026-09-03

### 新增

- IT 物资支持多仓库管理。仓库可维护编码、名称、所属组织、负责人、联系电话、地址、启用状态和备注。
- 新增仓库库存标签页、仓库目录、按当前仓库导出和仓库间库存调拨。
- 入库、库存调整、领用、归还、设备回收和离职回收均按仓库处理；库存扣减类领用和回收必须由操作人选择有效仓库。
- 新增 `warehouse_management` 权限模块。仓库及库存数据同时受仓库权限和组织数据范围约束。

### 数据库与兼容

- 新增增量迁移 `20260902_001_inventory_warehouses.sql`，创建仓库、仓库库存和调拨日志结构，并为领用、物资流转和采购入库补充仓库关联字段。
- 首次升级会在根组织下创建默认仓库“仓库1”（编码 `WH-001`），将原有库存汇总数量迁入该仓库；历史流转记录不改写。
- 默认仓库可编辑或停用，但不能删除。已有库存、领用、流转、采购或调拨记录的仓库不能删除。
- 旧版 `GET /api/state` 继续兼容读取；库存汇总字段继续保留，同时新增按仓库库存数据供新版前端使用。
- 修复 MySQL 8.0.46 对调拨日志仓库外键的初始化兼容性；仓库主键不允许更新，外键保留删除限制。

### 验证与回滚

- 补充仓库迁移幂等性、接口权限、组织数据范围、库存调拨事务、幂等键、默认仓库保护和前端仓库选择回归测试。
- 已修复库存型号弹窗中未关联表单字段的只读标签，避免浏览器无障碍检查报错。
- 修复 `DELETE` API 未进入统一鉴权和异常处理链的问题；删除默认仓库现在稳定返回 `409 STATE_CONFLICT`。
- 修复跨仓库归还至已有同型号库存时的重复主键冲突，归还事务会安全复用已有库存行。
- 回退应用前应确认没有新建的仓库库存、调拨或仓库关联流转；本迁移不自动删除业务数据。

## v2.0.16

发布日期：2026-09-02

### 修复

- 修复办公终端台账编辑时，界面虽然提示保存成功，但使用用户未实际变更的问题。
- 办公终端使用用户现在改为独立分配/归还命令提交，并与台账保存分离，避免只改到前端快照。
- 修复“使用人员 > 设备清单”中自定义品牌和型号时仍被强制关联库存型号的问题。
- “仅登记不扣减”现在允许自定义品牌、型号和库存品牌映射，不再要求已登记库存型号。
- 返还与列表查询补全了自定义设备的品牌、型号回退显示，避免记录为空。

### 验证与数据库

- 已补充前端保存链路、自定义登记分支和返回/列表回退显示的回归测试。
- 本版本不新增数据库迁移，不修改现有业务数据结构。

### 回滚

- 仅需回退应用代码至 `v2.0.15`；本版本没有需要回滚的数据库迁移。

## v2.0.15

发布日期：2026-09-01

### 修复

- 修复人员设备管理中选择“仅登记不扣减”时，后端将 JSON `false` 误判为需要扣减库存，
  导致零库存设备无法保存的问题。
- “仅登记不扣减”现在可以在库存数量为 0 时正常创建领用登记，不修改库存数量，也不生成库存扣减流水。
- 未传入 `stockAdjusted` 的旧客户端仍按原规则默认扣减库存，保持兼容性。

### 验证与数据库

- 增加单元回归测试和接口回归测试，覆盖零库存仅登记、库存数量、领用记录及扣减流水。
- 已通过 Python 编译检查、JavaScript 语法检查、51 项单元测试和隔离数据库接口回归测试。
- 本版本不新增数据库迁移，不修改现有业务数据。

### 回滚

- 仅需回退应用代码至 `v2.0.14`；本版本没有需要回滚的数据库迁移。

## v2.0.14

发布日期：2026-08-31

### 新增

- 在“使用人员”中新增“办理离职”操作，由具备使用人员修改权限且数据范围为全部或所属部门的账号执行。
- 办理离职要求填写离职日期、离职原因和备注，并逐项处理名下办公终端、显示屏和非资产物资。
- 每项资产或物资支持回收、转交他人或异常待处理；未处理或异常未说明时不能提交。

### 修复与强化

- 离职办理改为独立后端命令和独立事务，不再通过前端全量状态快照模拟归档。
- 离职事务会原子完成终端归还或转交、物资回收入库或转交、异常关闭、离职档案、操作日志、系统消息、账号解绑和会话撤销。
- 离职档案的设备快照记录每项处理方式、接收人员和处理说明，便于后续审计追溯。

### 数据库与配置

- 不新增数据库表或配置项，复用现有离职档案、流转日志、审计日志、通知、账号绑定和会话表。
- 现有账号、人员、资产、库存、工单、权限、日志和历史离职档案均保留。

### 验证与回滚

- 已补充离职办理前端入口、后端命令、完整处理校验、账号解绑和审计通知的回归测试。
- 回滚应用代码前应确认没有正在执行的离职办理事务；本版本没有新增数据库迁移。

## v2.0.13

发布日期：2026-08-31

### 修复

- 移除普通“使用人员”编辑页中的离职状态、离职日期、离职信息和备注输入框。
- 普通人员编辑仅允许维护在职或停用状态，避免用户误以为可以绕过设备回收直接完成离职归档。
- 保留“离职人员”页面中的离职资料、备注和离职时设备快照只读展示。
- 增加人员编辑页回归测试，防止离职字段被后续前端改动重新引入。

### 数据库与配置

- 不包含数据库迁移、配置变更或业务数据变更。
- 现有账号、人员、资产、工单、库存、流转记录、日志和离职档案均保留。

### 使用说明

- 普通人员维护：进入“使用人员”，新增或编辑人员基本信息，状态选择“在职”或“停用”。
- 设备处理：在人员设备管理中进行分配、领用和回收；库存变动和流转日志按现有事务流程记录。
- 离职查看：进入“离职人员”查看已归档资料及设备快照。
- 当前版本尚未开放独立的“办理离职”入口；不得通过人员编辑接口直接设置离职状态。完整离职办理应由独立的受控归档命令负责确认、回收、解绑、归档和审计。

### 验证与回滚

- 已通过 JavaScript 语法检查、Python 编译检查和完整 `tests.test_regressions` 回归测试。
- 回滚应用代码前应确认没有正在进行的更新任务；本版本不需要回滚数据库迁移。

## v2.0.12

发布日期：2026-08-29

### 修复

- 修复服务器更新控制服务可以检查到私有本机 Gitea 的版本，但点击更新时部署脚本没有继承
  HTTP 到 SSH 的仓库映射、重新访问需要登录的 HTTP 地址并失败的问题。
- 更新控制服务现在会把已校验的本机 Gitea HTTP/SSH 地址映射显式传给部署脚本；私有仓库
  的页面地址仍可保持 HTTP 地址，实际拉取统一使用部署账号已有权限的 SSH 地址。

### 数据库与配置

- 不包含数据库迁移或业务数据变更。
- 服务器仍需在应用 `.env` 同时配置 `DEPLOY_LOCAL_GITEA_HTTP_ORIGIN` 和
  `DEPLOY_LOCAL_GITEA_SSH_ORIGIN`；不应在项目地址、仓库或页面中保存账号、密码或令牌。

### 验证与回滚

- 新增部署子进程环境回归测试，覆盖 Gitea HTTP/SSH 映射传递。
- 回滚代码前，应先确认没有正在执行的更新任务；本版本不需要回滚数据库迁移。

## v2.0.11

发布日期：2026-08-29

### 修复

- 修复物资回收在归还最后一件或完整归还旧版领用记录时，先将数量写为 `0` 而触发
  `quantity > 0` 数据库约束、页面显示“服务器内部错误”的问题。
- 普通分配归还和旧版领用兼容归还均改为：剩余数量大于归还数量时递减；数量恰好相等时直接删除领用记录，不产生非法的零数量中间状态。
- 普通分配归还会先锁定并校验当前领用数量，领用记录缺失或数量不足时不会错误更新分配状态或重复回补库存。

### 数据库与配置

- 不包含数据库迁移或配置变更；已有账户、资产、领用、库存、工单、权限、审计和备份数据不受影响。

### 验证与回滚

- 新增完整归还不写入零数量的回归测试；已通过 Python 编译和 46 项回归测试。
- 在生产数据库中以自动回滚事务验证：数量为 `1` 的非资产领用记录可被直接删除，未触发检查约束且不会保留测试数据。
- 回滚应用代码前，应先确认没有使用本版本修复后的回收事务处理业务记录；本版本不需要回滚数据库迁移。

## v2.0.10

发布日期：2026-08-29

### 修复

- 更新控制服务支持从部署目录 `.env` 读取本机 Gitea HTTP 到 SSH 映射的两个非敏感地址；系统服务环境变量优先级更高。
- 在系统管理员集中管理 systemd 环境文件时，部署账号可维护同一应用目录下的映射地址，无需把 Gitea 账号、密码或访问令牌写入页面、仓库或配置文件。

### 数据库与配置

- 不包含数据库迁移；账户、资产、工单、权限、审计和备份数据不受影响。
- 如需私有本机 Gitea HTTP 项目地址更新，在 `/opt/office-asset-mgmt/.env` 增加 `DEPLOY_LOCAL_GITEA_HTTP_ORIGIN` 与 `DEPLOY_LOCAL_GITEA_SSH_ORIGIN`，两个变量必须成对设置。

### 验证与回滚

- 覆盖 `.env` 映射读取、精确来源匹配、来源不匹配不转换及不完整配置拒绝运行的回归测试。
- 回滚应用代码时仅需回退应用提交并移除 `.env` 中的两个映射变量；本版本没有需要回退的数据库迁移。

## v2.0.9

发布日期：2026-08-27

### 修复

- 修复私有本机 Gitea 使用 HTTP 项目地址检查版本时，由于仓库要求登录认证而返回 HTTP 502 的问题。
- 对精确匹配的本机 Gitea HTTP 来源，更新控制服务和实际部署脚本在服务器内部改用部署账号已有读取权限的 SSH 来源；页面继续显示原始 HTTP 项目地址。
- 仓库拉取失败提示改为中性说明，覆盖 GitHub 网络、项目地址和本机 Gitea 部署读取权限，避免误导为 HTTPS 专属问题。

### 数据库与配置

- 不包含数据库迁移；账户、资产、工单、权限、审计和备份数据不受影响。
- 可选环境变量 `DEPLOY_LOCAL_GITEA_HTTP_ORIGIN` 与 `DEPLOY_LOCAL_GITEA_SSH_ORIGIN` 必须成对设置，且只能填写协议、主机和端口。不得在项目地址或环境变量中保存用户名、密码或访问令牌。

### 验证与回滚

- 覆盖精确 HTTP 到 SSH 映射、来源不匹配不转换、缺少任一映射配置即拒绝运行、更新服务错误提示和部署脚本结构回归。
- 回滚应用代码时仅需回退应用提交并恢复更新服务环境文件；本版本没有需要回退的数据库迁移。

## v2.0.8

发布日期：2026-08-27

### 功能与修复

- 物资流转记录的备注支持独立更正：原始流转事务不再被覆盖，每次更正均保存更正后的备注、更正原因、操作人和时间，并写入审计日志。
- `/api/state` 保持兼容读取：流转记录新增原始备注、当前有效备注和更正历史；原有 `note` 字段继续返回当前有效备注。
- 同步与质量页面的同步来源、批次状态、质量问题严重性、规则、对象和处理状态均改为中文显示。
- 数据质量问题点击“解决”后必须填写处理结果；处理结果随问题状态一并保存，并写入 `data_quality_issue_resolved` 审计日志。
- 修复 Windows 初始化空数据库时 `CREATE DATABASE` 没有返回结果而导致部署脚本错误中断的问题。

### 数据库与配置

- 新增增量迁移 `20260827_001_flow_note_corrections_and_quality_resolution`：
  - 创建 `inventory_movement_note_correction`，用于追加式保存物资流转备注更正。
  - 为 `data_quality_issue` 增加 `resolution_result`，保存问题解决结果。
- 迁移通过信息架构检查保证可重试；不得手工修改已登记的历史迁移文件。
- 不修改现有资产、流转、工单、账号、权限、审计或备份数据。

### 验证与回滚

- 已覆盖 Python 与 JavaScript 语法检查、41 项结构回归、测试沙盒迁移验证，以及真实 HTTP 的权限、CSRF、审计和数据范围回归。
- 回滚应用代码前应评估是否继续保留新增的更正和处理结果数据；数据库迁移为仅新增对象和字段，不需要删除业务数据。

## v2.0.7

发布日期：2026-08-27

### 修复

- 修复版本更新在 GitHub HTTPS 拉取出现短暂连接中断时立即返回 HTTP 502 的问题。
- 更新检查和实际部署统一使用 HTTP/1.1，并对可恢复的网络失败进行最多三次短暂重试。
- 显式同步 `v*` 发布标签，避免发布标签缓存与远端修正后的标签元数据不一致。
- 拉取最终失败时返回“更新项目暂时无法通过 HTTPS 获取”的明确提示，不再把仓库传输问题误报为更新服务故障。

### 数据库与配置

- 不包含数据库迁移；账户、资产、工单、权限、审计和备份数据不受影响。
- 可选环境变量：`DEPLOY_GIT_FETCH_ATTEMPTS`（1-5，默认 `3`）和
  `DEPLOY_GIT_FETCH_RETRY_SECONDS`（1-30，默认 `2`）。

### 验证与回滚

- 覆盖 HTTPS 拉取重试、发布标签同步和更新服务错误映射的回归测试。
- 回滚代码时仅需回退应用提交；本版本没有需要回退的数据库迁移。

## v2.0.6

发布日期：2026-08-26

### 修复

- 修复库存回收页面点击已选设备后仍提示“未选定”的问题，兼容数字和字符串格式的设备标识。
- 修复多条物资部分回收失败后的重试逻辑，避免重复回收已处理记录。
- 为没有历史分配记录的旧版显示屏和非资产使用记录增加事务化兼容回收接口。

### 数据库与配置

- 不包含新的数据库迁移或环境变量变更；现有账户、资产、工单、权限和审计数据不受影响。
- 兼容回收会写入分配历史、库存变动和审计记录，库存已调整的旧记录会同步恢复库存。

### 验证与回滚

- 已执行 35 项回归测试，并通过 Python 编译、JavaScript 语法和 Git 差异检查。
- 回滚代码时仅需回退应用提交；本版本没有需要回退的数据库迁移。

## v2.0.5

发布日期：2026-08-25

### 修复

- 补齐夜间模式残留的同色文字和浅底组件，覆盖组织树、人员页、登录页、摘要卡片和提示块的可读性问题。
- 保持原有布局、交互和主题结构不变，仅修正暗色模式下的背景与前景色对比。

### 数据库与配置

- 不包含数据库结构、迁移或环境变量变更；现有账户、资产、工单、权限和审计数据不受影响。

### 验证与回滚

- 已执行前端回归测试，并确认静态资源引用已更新，浏览器可通过新版本号重新获取样式。
- 回滚代码时仅需回退应用提交；本版本没有需要回退的数据库迁移。

## v2.0.4

发布日期：2026-08-24

### 修复

- 恢复原有页面布局、组件样式和主题配色，移除会覆盖全部页面的黑白单色主题规则。
- 设置页切换分区时仅替换标签和内容区域，避免整页重新渲染造成的点击闪烁。
- 夜间模式的页面、表单设计区和流程设计区使用纯黑底色；离职设备快照与资产标签改为深色表面和浅色文字，避免浅底白字。
- 保留日间模式的原有设计风格，并确保表单字段和设计器文字使用可读前景色。

### 数据库与配置

- 不包含数据库结构、迁移或环境变量变更；现有账户、资产、工单、权限和审计数据不受影响。

### 验证与回滚

- 已执行前端回归测试，并确认 `127.0.0.1:8011` 已加载本版本的静态资源。
- 回滚代码时仅需回退应用提交；本版本没有需要回退的数据库迁移。

## v2.0.3

发布日期：2026-08-24

### 修复

- 修复 v1.2.3 历史生产库升级时，迁移登记前错误要求存在 `auth_bootstrap_guard` 而导致更新自动回滚的问题。
- 新增最先执行的幂等安全兼容迁移：创建认证启动保护表，并在缺失时补齐 `auth_session.ip_address` 与 `auth_session.user_agent`。
- 基线接管继续校验认证表与核心业务表，防止把不兼容或空白数据库错误标记为已完成历史基线。

### 部署

- 已有 v1.2.3 生产库仍需先完成备份，并临时设置 `MIGRATION_ADOPT_BASELINE=legacy-20260813`。
- 升级成功后删除该环境变量并执行迁移校验；不需要执行 `database/bootstrap/` 中的历史重建脚本。

## v2.0.2

发布日期：2026-08-21

### 修复

- 修复已有生产数据库升级到 v2 时因缺少 `schema_migration` 登记表而在迁移阶段回滚、页面保持旧版本的问题。
- 新增经过校验的一次性历史基线接管：仅允许 `legacy-20260813`，并在写入迁移登记前检查组织、人员、资产、审计、认证、库存和备份关键表。
- 保持默认保护：旧业务库未显式设置基线时仍会拒绝迁移，不会重放 `database/bootstrap/01_schema.sql` 或自动覆盖业务数据。
- 更新部署脚本为显式运行一次性 `migrate` 容器；迁移原始输出会保留在 `office-asset-gitea-webhook` 的 journal 中，方便定位失败原因。
- 增加 Docker Compose 数据库备份脚本，默认写入部署账户私有目录，并生成压缩备份和 SHA-256 校验文件，避免因应用目录由 root 持有而导致备份失败。

### 数据库与配置

- 不新增业务表或业务字段；会使用现有的 v2.0.0 增量迁移。
- 对已有且没有 `schema_migration` 的生产库，必须先备份并确认历史结构，再在服务器 `.env` 临时设置：

```dotenv
MIGRATION_ADOPT_BASELINE=legacy-20260813
```

- 迁移成功后删除该环境变量，并执行迁移校验。新安装和已登记迁移的数据库不需要设置它。

### 验证与回滚

- 已增加迁移基线、部署脚本和 Compose 备份路径回归测试；发布前执行 Python 编译、单元测试、Shell 语法和前端 JavaScript 语法检查。
- 部署前先使用 `deploy/scripts/backup_compose_database.sh` 生成并校验备份。迁移、构建或健康检查失败时，部署脚本会恢复旧应用提交，并绕过旧版的迁移依赖直接重启旧 app；数据库迁移不自动反向执行，必要时从已验证备份恢复。

## v2.0.1

发布日期：2026-08-21

### 仓库整理

- 按职责重新归类数据库初始化脚本、增量迁移、手工维护 SQL、Windows 部署脚本、迁移工具、集成回归测试和项目文档。
- 新增各目录说明与文档索引，根目录 README 明确代码、数据库、部署、测试和文档的入口。
- 保留根目录 `deploy.ps1`、`migration_runner.py`、`run_mysql_utf8.py` 和 `qa_security_regression.py` 兼容入口，现有自动化命令可继续使用。
- Docker Compose、Docker 初始化、CI、测试与部署文档已切换到新的目录路径。
- 修复 Windows 部署脚本中 `DbName` 参数与别名冲突；`-DbName` 仍保持兼容。

### 数据库与配置

- 不包含数据库结构变更，不需要执行新的迁移。
- 不新增环境变量，不修改账户、密码、资产、工单、日志或备份数据。

### 验证与回滚

- 已执行 Python 编译、单元与结构回归、前端 JavaScript 语法、PowerShell 语法、迁移文件完整性、文档链接和暂存安全扫描。
- Docker Compose 实际启动、Bash 脚本语法及需要真实数据库凭据的集成回归，应在具备对应环境的验收阶段执行。
- 回滚到 `v2.0.0` 仅恢复原有仓库目录布局；不会回滚数据库内容或任何业务数据。

## v2.0.0

发布日期：2026-08-21

### 功能与架构

- 资产写入从全量状态快照转为资源接口和原子命令：入库、领用、归还、办公终端分配和归还均在独立事务中执行，并支持幂等键和审计。
- 保留 `GET /api/state` 兼容读取；`PUT /api/state` 已退役，返回 `405 STATE_WRITE_RETIRED`。
- 引入 `office_asset/` 领域模块、SQL 仓储、权限和服务层，降低 `server.py` 的职责集中度。
- 增加同步暂存、导入追踪和数据质量审计能力。
- 增加办公终端设备流转记录及详情时间线，覆盖分配、归还和生命周期变化。
- 权限体系支持中文角色和模块名、角色类别、用户覆盖权限、查看/新增/修改/删除/审批/导出操作，以及全部/所属部门/本人/本人提交/负责工单等数据范围。
- 超级管理员由服务端角色标记判定；现有管理员获得全部权限，新建超级管理员仅可由超级管理员创建。

### IT 服务管理

- 工单模块名称统一为“工单”，并增加服务表单设计、人员和部门自动预填、表单与流程绑定。
- 增加变更管理、问题管理、知识库、SLA 计时、自动通知和多级审批。
- 服务管理页面支持常用新建工单和消息提醒入口，流程处理人员会在表单流程中体现。

### 前端与体验

- 重构总览、设置和服务管理页面的信息层级；设置采用分组卡片式布局。
- 优化工单与服务管理按钮状态、物资流转状态颜色、列表容器层级和暗色主题。
- 更新静态资源缓存版本为 `20260821-v2.0.0`，减少旧脚本和样式缓存导致的页面异常。

### 数据库与部署

- 引入 `schema_migration` 迁移登记表和 SHA-256 校验和，生产升级只应用 `migrations/` 中的新文件。
- 新增从 `20260814_001_itil_governance.sql` 到 `20260820_001_computer_movement_history.sql` 的增量迁移。
- `deploy.ps1` 增加 `-Database`/`-DbName`、目标库名校验、空库初始化与已有库显式基线采用保护。
- Docker Compose 增加 `migrate` 服务；应用仅在数据库健康且迁移成功后启动。
- Docker 空库初始化会登记 `legacy-20260813`，已有数据库缺少登记表时拒绝自动采用基线。

### 验证与回滚

- 已执行 Python 编译、前端 JavaScript 语法检查、迁移校验和接口安全回归。
- Docker Compose、Bash 脚本和浏览器关键路径应在具备对应运行环境的部署或验收阶段补充验证。
- 升级前必须备份。代码回退不会撤销迁移；数据库异常需从已验证备份恢复。

## v1.2.3

发布日期：2026-08-13

### 功能变更

- 设置页的版本更新增加“发行版”和“Beta 版”更新通道。发行版仅列出稳定的
  `vMAJOR.MINOR.PATCH` 版本；Beta 版仅列出 `vMAJOR.MINOR.PATCH-beta.N` 等预发布版本。
- 未指定更新通道时，默认使用 Beta 版；切换项目地址或更新通道后必须重新检查，避免使用
  旧检查结果执行更新。
- 优化版本更新面板布局：项目地址、版本状态、更新通道、目标版本和版本说明层级更清晰，
  宽屏双列呈现，窄屏自动收拢。
- 增加页面内容进入、面板悬停和按钮反馈动效，并遵守系统“减少动态效果”偏好。

### 数据库与配置

- 不需要数据库迁移或新增环境变量。
- 服务器更新控制服务随应用版本升级后支持 `releaseChannel=release|beta` 参数。

### 验证与回滚

- 验证发行版通道仅返回稳定注释标签，Beta 版通道仅返回预发布注释标签。
- 回滚到 `v1.2.2` 不会修改物资、流转记录、账号、数据库备份或系统设置数据；版本更新页面将
  不再显示更新通道选择。

## v1.2.2

发布日期：2026-08-13

### 修复

- 修复物资流转记录关键词、人员和来源去向筛选在连续输入或中文输入法组合输入时提前
  应用筛选、重绘列表的问题。文本输入现在仅保存草稿，点击“查询”或按 Enter 后才应用。
- 库存关键词搜索调整为同一提交式交互，避免输入过程中页面重绘导致焦点或输入状态异常。
- 操作日志的关键词和人员输入不再在失焦时自动请求接口，只会在点击“应用筛选”或按
  Enter 后查询，避免未输入完成时发出不完整查询。

### 数据库与配置

- 不需要数据库迁移，也不需要新增配置项。

### 验证与回滚

- 连续输入物品、人员、来源去向等关键词时，列表在查询前保持不变；点击“查询”或按
  Enter 后结果、数量和 Excel 导出应同步更新。
- 回滚到 `v1.2.1` 不会修改或删除任何物资流转记录、备注、库存或操作日志数据。

## v1.2.1

发布日期：2026-08-12

### 功能变更

- 物资流转记录新增多条件筛选：物品、品牌型号和备注关键词，物资类型，业务类型，
  业务分类，关联人员姓名或编号，来源或去向，以及开始和结束日期。
- 页面显示当前筛选结果数量，支持一键清除全部筛选条件。
- 新增“导出当前结果”，导出的 Excel 包含流转时间、业务类型与分类、物品、数量、
  库存影响、调出方、接收方、关联人员、触发动作和备注。

### 数据库与配置

- 不需要数据库迁移，也不需要新增配置项。

### 验证与回滚

- 升级后分别按物品、人员和日期筛选，确认结果数量和导出 Excel 一致。
- 回滚到 `v1.2.0` 不会删除任何 `inventory_movement_log` 流转记录或备注。

## v1.2.0

发布日期：2026-08-11

### 功能变更

- 将“物资流转中控”调整为“物资流转记录”，页面仅保留统一的物资流转记录表。
- 移除该页面的手工登记表单、统计卡片和筛选工具；物资入库、领用、归还、库存调整、
  人员设备回收等动作仍由对应业务页面自动写入流转记录。
- 记录表根据动作自动展示“业务类型”和“业务分类”，包括库存入库、领用发放、
  归还回收和库存管理等分类。
- 每条流转记录新增可直接编辑并保存的备注栏，用于补充工单号、交接情况或处理说明。

### 数据库与配置

- 不需要数据库迁移，也不需要新增配置项。
- 升级前建议按既有流程创建数据库备份；升级后抽查历史流转记录和备注保存功能。

### 回滚

- 回滚到 `v1.1.0` 将恢复原“物资流转中控”录入界面；已保存的流转记录和备注保留在
  `inventory_movement_log` 中，不会因前端回滚丢失。

## v1.1.0

发布日期：2026-08-07

### 功能与安全修复

- 修复登录会话过期后旧主页仍出现在登录页下方的问题，并补充静态资源缓存刷新。
- 增加请求体大小限制、密码长度上限、按 IP 和账号的登录频率限制及 HTTP 429 响应。
- 增强管理员首次初始化并发保护，记录会话创建时的客户端 IP 和 User-Agent。
- 严格校验状态快照字段和正数版本号，避免不完整数据覆盖数据库。
- 保持更新流程为管理员手动选择版本；push webhook 只记录事件，不自动部署。
- 版本列表仅接受已合并到 `main` 的 SemVer 注释标签、匹配的
  `VERSION_NOTES.md` 条目和稳定版本；设置页显示版本说明并只允许选择更高版本。
- 设置页版本更新增加项目地址输入框，可填写 GitHub 或内网 Gitea Git 地址检查可用版本；
  留空时使用服务器部署目录的默认 `origin`。
- 更新控制服务默认使用 HTTPS，并支持指定 CA 文件；部署失败时恢复之前的提交和应用镜像。
- 备份脚本改为临时文件加原子移动，增加 `--skip-lock-tables`、`--no-tablespaces` 和
  `--hex-blob`，并避免在进程参数中暴露 MySQL 密码。
- 补充库存外键、旧引用清理和办公终端术语迁移；阻止删除仍被办公终端引用的库存型号。
- GitHub Actions 仅执行源码校验，不再包含服务器 SSH 部署步骤。

### 数据库迁移

- 新增 `database/21_security_hardening.sql` 和 `database/22_update_repository_setting.sql`。
- 该迁移会补充 `auth_bootstrap_guard`、旧安装缺少的会话来源字段，清理无效库存引用，
  并为库存关联补充必要的索引和外键；同时增加可选的 `update_repository_url` 设置项。
- 生产升级前必须完成数据库备份，在测试库验证迁移，再由管理员手动执行迁移。
  更新脚本不会自动执行 SQL 迁移。

### 配置变更

- 生产环境设置 `AUTH_COOKIE_SECURE=true`，通过 HTTPS 访问应用。
- 更新控制服务配置 `DEPLOY_TLS_CERT_FILE`、`DEPLOY_TLS_KEY_FILE`、
  `UPDATE_SERVICE_CA_FILE` 和独立的控制令牌。
- 仅隔离的本地开发环境可以显式启用 HTTP 更新控制服务。

### 回滚与验证

- 更新前保留数据库备份和当前应用提交 SHA。
- 更新后检查 `/api/health`、应用和数据库容器状态、登录、版本检查及备份功能。
- 若构建、启动或健康检查失败，部署脚本会恢复前一提交并重建旧应用镜像；
  数据库迁移失败仍需使用备份或人工回滚。

## v1.0.0

发布日期：2026-08-05

### 功能

- 提供组织、人员、办公终端、IT 物资库存、操作日志和数据库备份管理。
- 设置页支持检查 Gitea 已发布版本、查看版本说明并手动选择目标版本。
- 支持亮色和暗色主题切换。

### 发布要求

- 使用 `vMAJOR.MINOR.PATCH` 格式的 SemVer 注释标签。
- 推送代码不会自动部署；生产更新必须由管理员在设置页确认。
- 生产更新前备份数据库，并按版本说明执行必要的数据库迁移。
