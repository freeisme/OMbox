# 版本更新说明

本文件记录每个可部署版本的功能变更、数据库影响、配置要求和回滚提示。
可部署版本必须使用未占用的 SemVer 注释标签，并在标签对应提交中包含本文件的同名
版本标题，例如 `v1.1.0`。

写作约定：

- 只写本次改了什么、有什么影响、怎么验证、怎么回滚，**保持简洁**，不复述实现细节；
- **不出现任何真实业务数据**——人员姓名、工号、设备编码、组织与仓库名称、客户名称一律用
  泛化示例（如「某部门」「IT-PC-01」），详细来龙去脉写到 `docs/development/` 下的文档里；
- v2.31.0 起旧前端已整体删除，早期条目里的「回滚到旧前端 / 从 `MIGRATED_VIEWS` 移除」
  操作已不再适用。

## v3.0.7

发布日期：2026-09-22

### 变更

- **加固发布前脱敏扫描**：`local-machine-path` 规则原来只匹配双反斜杠写法（JSON / Python 字符串里的转义形式），
  文档里用单反斜杠或正斜杠直接写本机盘符路径会漏过。现在单反斜杠、双反斜杠与正斜杠三种写法都会命中。

### 数据库影响

无。

### 验证

对当前 `main` 与全部 7 个已发布提交重跑扫描均通过；用构造样例确认新规则能拦下
单反斜杠、双反斜杠、正斜杠与大小写盘符四种写法。

### 回滚

`git revert` 本提交即可恢复旧的宽松匹配（不建议，会漏掉单反斜杠写法）。

## v3.0.6

发布日期：2026-09-22

### 变更

- **新增：机房设备的运维字段**。台账表单与列表增加「用途」「远程访问地址」「CPU」「内存」「硬盘」，
  与原有的设备名称、品牌型号、设备类型、占用高度、SN / ST、固资编码、使用人、状态、备注成套；
  Excel 导入同样识别这几个表头。数据库迁移 `20260922_001_datacenter_device_details.sql`
  为 `datacenter_device` 增加 `purpose`、`remote_access`、`cpu`、`memory`、`disk` 五列（均为空字符串默认值）。
- **修复：编辑 / 删除机房设备一直 404**。路由把路径段数写成了 5 / 6，
  而前端用的是 `PUT /api/datacenter-devices/{id}` 与 `POST /api/datacenter-devices/{id}/remove`（4 / 5 段），
  两条路径从来没有匹配成功——表现为"改完点保存没反应"。现已按前端路径修正并补上回归用例。
- **调整：复制只沿用非唯一的配置项**。网络设备复制时带过品牌型号、设备类型、占用高度、用途、
  远程访问地址、CPU、内存、硬盘、使用人，设备编号、名称、SN / ST、固资编码留空手填；
  机柜复制同样只带过所属机房与高度，编码与名称留空。备注一律不复制。

### 数据库影响

新增 5 个字段列（可空、默认空字符串），不改动既有数据；升级时由迁移自动执行。

### 验证

接口与界面实测：编辑设备填入五个新字段后保存成功并回显在列表；复制设备得到的记录带全套配置
但编号 / 名称 / SN / 固资为空，填好编号名称即可保存；结构回归 143 项、前端类型检查与构建、
推送前脱敏扫描通过。

### 回滚

`git revert` 本提交；如果已经执行过迁移，回滚代码后多出的 5 列留着不影响运行（如需清理可手工
`ALTER TABLE datacenter_device DROP COLUMN ...`）。

## v3.0.5

发布日期：2026-09-22

### 变更

- **新增：机房 / 弱电间与机柜可以删除**。此前只能新增和修改。删除会写入操作日志，
  可以填写删除原因，并带引用保护：
  - 机房下还有机柜时拒绝，提示先删除或转移机柜；
  - 机柜内还有已上架设备、或登记着设备端口与链路时拒绝，提示先下架 / 清理；
  - 关联过巡检任务的机房或机柜拒绝删除，避免破坏巡检历史。
  对应接口：`DELETE /api/inspection/sites/{id}`、`DELETE /api/inspection/racks/{id}`。
- **调整：网络设备与机柜的「复制」改为除备注外全部复制**。设备编号、名称、SN / ST、
  固资编码、品牌型号、设备类型、占用高度、使用人，以及机柜的编码、名称、所属机房、高度
  都会带过来，只有备注留空；编号或名称与已有记录重复时，保存会被拒绝并提示
  「数据唯一标识已存在，请检查重复的名称、编码或编号。」，手动改成唯一值即可保存。
  唯一例外：原设备处于「上架」状态时，复制出来的设备状态回到「未上架」——
  上架状态只能由机柜视图的上架操作写入。

### 数据库影响

无（只新增删除接口与界面按钮，表结构不变）。

### 验证

接口层实测：删除仍挂机柜的机房返回 409 并提示原因；删除空闲机房、删除机柜返回 200，
列表随之更新。界面层实测：复制机柜后编码与名称与来源一致、备注为空，直接保存被拒绝
（对话框保持打开），把编码改成唯一值后保存成功并出现在列表。
结构回归 141 项、前端类型检查与构建、推送前脱敏扫描均通过。

### 回滚

`git revert` 本提交即可；回滚后机房与机柜只能新增 / 修改，复制会清空编号与名称等唯一字段。

## v3.0.4

发布日期：2026-09-22

### 变更

- **新增：网络设备 / 服务器「复制」**。台账行操作列增加「复制」，沿用被复制设备的
  型号库、品牌型号、设备类型、占用高度、使用人与备注，只清空设备编号、设备名称、
  SN / ST 与固资编码这些唯一值，状态回到「未上架」，打开对话框时给出补填提示。
  批量上同型号设备时不用再逐项重录。
- **新增：机柜「复制」**。巡检页「机房与机柜」的机柜行增加「复制」，沿用所属机房、
  高度与备注，只清空机柜编码与名称。

### 数据库影响

无（复用既有的新增接口，只是把表单预填好）。

### 验证

结构回归与前端类型检查、构建通过；本地实测：复制网络设备后新设备沿用
「华为 S5731-H48T4XC / 网络设备 / 1U」并保持未上架、编号为空；
复制机柜后新机柜沿用所属机房与 42U 高度、编码与名称为空；保存后均正确出现在列表。

### 回滚

`git revert` 本提交即可，回滚后操作列不再有「复制」按钮。

## v3.0.3

发布日期：2026-09-22

### 变更

- **修复：机房巡检无法新建机柜**。`POST /api/inspection/racks` 一直存在，但前端迁移到 Vue 后
  只剩列表、没有新增入口，表现为「机柜建不出来」。现已补回：
  - 巡检页「机房与机柜」新增「＋ 新增机房 / 弱电间」与「＋ 新增机柜」，
    机柜必须先选所属机房，前端先校验，避免直接撞后端 400；
  - 「巡检模板」新增「＋ 新增模板」，可增删检查项（分类 / 检查项 / 检查方法 / 结论类型 /
    单位 / 正常范围 / 是否必填）；
  - 机房与机柜两张表补上「编辑」（后端 PUT 一直存在），机房表增加「机柜数」列；
  - 机柜视图工具栏增加「新增机柜」，通过 `?tab=sites` 直达机房与机柜页签。
- **修复：巡检模板「检查项」列恒为 0**。列表接口返回的是 `itemCount`，页面读的是 `items`
  （列表不返回明细），改为优先读 `itemCount`。

### 数据库影响

无。

### 验证

结构回归通过；前端类型检查与构建通过；本地起服务实测：新建机房（含弱电间类型）、
新建机柜、新建模板均成功并即时出现在列表，模板检查项数量正确显示（示例：1 / 16 / 14）。

### 回滚

`git revert` 本提交即可；回滚后机房、机柜、巡检模板只能查看，无法在界面新建。

### 已知未实现（不在本次范围）

- 服务表单设计器与审批流程：后端接口存在、旧前端曾有界面，目前前端仍无入口，工作量较大需单独安排。

## v3.0.2

发布日期：2026-09-21

### 变更

- **文档去内部化**：文档、界面文案与数据库设置说明不再出现内部 Git 备份服务的名称，
  统一表述为「内网 Git」；对应的部署文档合并为 `docs/deployment/update-service.md`
  （更新服务、手动更新闸门、发布与回滚）。
- **移除内部基础设施文件**：删除内部 Git 备份库的自建编排与示例配置——那是运维单独维护的
  基础设施，不属于本项目；仓库里只保留更新服务本身。
- **版本说明精简**：只保留 v3.0.x（旧版本已随仓库重建作废）。

### 数据库影响

无。

### 验证

结构回归与前端构建通过；仓库内除运行时标识（部署脚本路径、systemd 单元名、Webhook 签名头）外
不再出现内部 Git 服务名称。

### 回滚

`git revert` 本提交即可；删除的两个编排文件不影响运行中的环境（实际服务不在部署目录内）。

## v3.0.1

发布日期：2026-09-21

### 变更

- **修复：使用人员名下物资不显示类型**。服务端 `/api/state` 下发人员名下的显示屏与非资产物资时
  只带 `typeId`、没带 `typeName`，前端只能统一兜底成「物资」。现在两者都带上类型名
  （显示屏 / 鼠标 / 键盘 / 拓展坞 / 鼠标垫 / 笔记本支架 / 键鼠套装 / 有线键鼠…），
  类型字典里查不到时显示屏回落「显示屏」、非资产物资回落「非资产设备」。
  影响面：使用人员列表「名下设备」列、设备弹窗、CSV 导出。

### 数据库影响

无。

### 验证

本地库实测 160 条人员物资（104 显示屏 + 56 非资产物资）全部带类型名、0 条缺失；
浏览器「名下设备」列不再出现「物资」；结构回归新增断言守住该字段。

### 回滚

`git revert` 本提交即可，接口字段为新增，旧前端不受影响。

## v3.0.0

发布日期：2026-09-21

### 变更

- **更名**：项目更名为 **OMbox · 运维（O&M）盒子**；系统设置里的默认系统名同步改为
  「OMbox运维（O&M）盒子」（`database/bootstrap/19_auth_and_settings.sql` 的 `app_name` 初值）。
- **仓库重建**：以单个初始提交重建仓库，历史中不再保留早期提交里的真实业务数据；
  版本号从 v3.0.0 重新起，旧版本标签全部作废。
- **README 重写**：按 OMbox 重新介绍定位、能力、部署方式与文档入口。
- **测试数据约定**：测试与示例一律使用虚拟信息（张三 / 李四 / 测试部门 / IT-PC-01 等），
  不引用真实人员、设备编码、组织与仓库名称。

### 数据库影响

无结构变更。默认系统名只影响**全新部署**；已有库沿用 `system_setting.app_name` 里的现值，
需要改名时在「设置 → 系统设置 → 系统名称」修改。

### 验证

脱敏扫描通过；单元与结构回归通过；`vue-tsc` 与 `vite build` 通过；按 Docker 初始化流程
重建空库核对：仅权限字典与参考数据有内容，人员、终端、账号、审计日志等业务表均为 0 条。

### 回滚

历史已重建，无法 `git revert` 回到 v2.x；回退需部署旧版本包并从备份恢复数据库。
