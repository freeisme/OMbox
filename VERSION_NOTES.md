# 版本更新说明

本文件记录每个可部署版本的功能变更、数据库影响、配置要求和回滚提示。
可部署版本必须使用未占用的 SemVer 注释标签，并在标签对应提交中包含本文件的同名
版本标题，例如 `v1.1.0`。

写作约定：

- 只写本次改了什么、有什么影响、怎么验证、怎么回滚，**保持简洁**，不复述实现细节；
- **不出现任何真实业务数据**——人员姓名、工号、设备编码、组织与仓库名称、客户名称一律用
  泛化示例（如「某部门」「IT-PC-01」），详细来龙去脉写到 `docs/development/` 下的文档里；
- v2.31.0 起旧前端已整体删除，早期条目里的「回滚到旧前端 / 从 `MIGRATED_VIEWS` 移除」
  操作已不再适用。

## v3.0.16

发布日期：2026-09-24

### 变更

补齐 v3.0.15 里明确"本版不做"的三项拓扑图交互，**纯前端改动，不改接口与数据库**：

- **撤销 / 重做布局**：工具栏新增「撤销」「重做」（快捷键 `Ctrl+Z` / `Ctrl+Y`，macOS 用 `Cmd`），
  最多回退 50 步；拖动节点与「重新布局」都会入栈。栈里只记节点坐标——端口与链路是服务端数据，
  不进撤销栈（删链路仍走「链路属性 → 删除链路」）。
- **框选与多选整体拖动**：在画布空白处按住拖动拉框，框内设备一起选中（画布上以虚线边框标出），
  拖动其中任意一台即整组平移；`Shift` / `Ctrl` + 单击可以加减单台设备，单击空白清空选择。
- **拖拽连线**：节点右边缘新增连线手柄，按住拖到另一台设备上松手即弹出「连接两台设备」，
  自动预选两端的第一条空闲端口（该端没有空闲端口时给出提示），确认后建立链路。

### 数据库影响

无（复用 `rack_cable_run` / `topology_node_position` 与既有接口）。

### 验证

浏览器实测（本机开发副本库，Playwright 驱动真实 DOM 事件）：

- 拉框选中 2 台设备 → 拖动任意一台，两台坐标同步变化（`translate(80,56)`、`translate(310,56)` →
  `translate(144,96)`、`translate(376,96)`）；
- 「撤销」回到拖动前坐标，「重做」再次应用；
- 从 A 设备连线手柄拖到 B 设备，橡皮筋与落点高亮正常，弹窗预选 `GE1/0/10`（`GE1/0/1` 已被占用而自动跳过），
  提交后画布出现第 2 条链路，两条并行链路的走线分别落在 `mid=270` / `mid=288`（并排不重叠）。

`python -m unittest tests.test_regressions` 173 项通过；`vue-tsc --noEmit` 与 `vite build` 通过；
`tools/scan_release_safety.py --rev v3.0.16` 通过。

### 回滚

`git revert` 本提交即可回到 v3.0.15 的交互（点选式连线 + 单节点拖动）。

## v3.0.15

发布日期：2026-09-24

### 变更

**网络拓扑页从只读展示变成可以直接编辑**（按用户确认的交互实现，功能参照 EVE-NG 的思路，实现为原创代码）：

- **连线向导**：`新建链路` → 点 **A 端设备** → 选端口或新建端口 → 点 **B 端设备** → 选端口或新建端口 →
  填介质 / 长度 / 标签 / 状态 / 备注。顶栏始终显示当前步骤与已选的 A/B 端，可随时取消；
  B 端选同一台设备、选中已被占用的端口会在前端先拦下（后端规则不变）。
- **端口弹窗四个页签**：端口列表（看状态、速率、IP/VLAN、对端，向导模式下点「选择」即选定）、
  **按型号生成**（型号库端口模板，或**复制另一台已配好端口的设备**）、**批量生成**
  （如 `GE1/0/{n}` 生成 1–24，可调范围/步长/每行，一次最多 256 个）、**手工添加**（逐条新增/编辑）。
- **链路属性**：点画布上的连线即选中它，右侧可改介质 / 长度 / 标签 / 状态 / 备注，也能删除。
- **展示优化**：连线按介质着色（电口、光纤、DAC、电源、Console 各自配色）；同一对设备之间的多条链路
  （堆叠 / LACP）横向错开显示而不是叠成一条；可选「正交走线 / 直线走线」；链路标签显示
  「A 端口 → B 端口」（有标签时显示标签）并可整体开关；拖动节点支持网格吸附；
  新增「只看选中设备相关」聚焦模式（选中设备及其一跳邻居）。
- **新增两个导出**：链路清单 CSV（A/B 端设备与端口、介质、长度、标签、状态、备注）与
  拓扑节点清单 CSV（设备、型号、类别、机房、机柜、起始 U 位、端口数、已连端口、层级）；
  原有 SVG / PNG / 打印保持不变。
- **端口新增两个可选字段**：IP/掩码（或管理地址）与 VLAN，用来补充台账信息，都不必填。
- 权限与审计沿用 `rack_layout` 模块（查看 / 创建 / 修改 / 删除），端口与链路变更继续写审计，不新增权限项。

本版本同时把此前已完成但未提交的「端口复制 / 批量生成」后端接口与新增端口字段一并纳入版本，
否则新界面调用的接口在发布版里不存在。

### 数据库影响

新增迁移 `database/migrations/20260924_002_rack_port_optional_fields.sql`：给 `rack_device_port`
增加 `ip_address VARCHAR(64)` 与 `vlan VARCHAR(32)` 两个非空默认空串的列，不改写历史数据。

### 验证

- 全新空库按 `bootstrap/` → `migrations/` 顺序初始化，全部迁移执行并 `--verify` 通过；
- 浏览器实测（本机开发副本库）：新建链路 → 点 A 端设备 → 批量生成 24 个 `GE1/0/{n}` 端口 →
  选端口 → 点 B 端设备 → 批量生成端口 → 选端口 → 建立链路成功；画布出现 1 条链路并显示
  「GE1/0/1 → GE1/0/1」，点连线可打开链路属性面板；
- `python -m unittest tests.test_regressions` 172 项通过；`vue-tsc --noEmit` 与 `vite build` 通过。

### 回滚

`git revert` 本提交（拓扑页回到只读展示）。数据库侧如需回退，先确认没有依赖这两个字段的导出：
`ALTER TABLE rack_device_port DROP COLUMN vlan, DROP COLUMN ip_address;`

## v3.0.14

发布日期：2026-09-24

### 变更

补齐第三方开源许可的随附与核查，本版本**不改变任何业务逻辑与数据库结构**。

- 新增 `THIRD_PARTY_NOTICES.md`：列出随前端构建产物分发的第三方组件（当前 55 个：54 个 MIT、
  1 个 BSD-3-Clause）的版本、许可、版权声明与许可全文。标记块由
  `tools/generate_third_party_notices.py` 从 `frontend/package.json` 的生产依赖生成，
  手写部分保留「借鉴与灵感来源」与「再分发与后续维护要求」。
- 该文件同时说明与外部项目的关系：**NetBox**、**Rackula** 仅作为数据模型与交互的参考，
  仓库未复制其代码、模板、图标或素材；早期内置的 NetBox `devicetype-library` 型号抓取工具
  已在 v2.14.0 删除，回归测试会阻止它回来。
- `LICENSE` 与 `README.md` 增加指向该清单的说明：本软件按专有内部许可发布，第三方组件权利
  不变；再分发（含打包镜像、复制到其他组织、交付源码）必须保留 `LICENSE` 与本清单。
- `Dockerfile` 把 `LICENSE`、`THIRD_PARTY_NOTICES.md` 复制进运行镜像，避免镜像里打包了
  MIT 许可的代码却没有随附声明。
- `docs/development/release-safety.md` 增加「第三方代码与许可」规则：只接受宽松许可、
  参考外部项目只借鉴思路不复制素材、改依赖后要重新生成清单。

### 数据库影响

无。

### 验证

- `python -m unittest tests.test_regressions` 166 项通过（新增 5 项：清单覆盖生产依赖、
  不含 GPL/AGPL/LGPL/SSPL、服务端仍只用 Python 标准库、Dockerfile/README/LICENSE 引用存在）；
- `python tools/scan_release_safety.py --rev v3.0.14` 通过；
- 人工核对：仓库未提交任何第三方图片、字体或图标；`office_asset/` 与 `server.py` 的全部
  import 都是标准库或本仓库模块。

### 回滚

`git revert` 本提交即可（纯文档与镜像内容变更，不影响运行逻辑、数据库与既有数据）。

## v3.0.13

发布日期：2026-09-23

本次同时携带未单独发布的 `v3.0.12`（下拉选项去括号、自定义物资与库存型号并存、会话改为无操作超时）。

### 变更

- **设备上架不再受“设备类型”枚举限制**：台账放开了自定义设备类型，但机柜上架仍按旧的 12 类枚举校验，
  自定义类型的设备一上架就报「设备类型无效」。现在上架/改位置与台账用同一套规则——常见写法
  （如 server、network、patch-panel）归一化成预设值，其余按原文保留（最长 64 字符）；
  前端设备类型下拉也支持直接输入。
- **离职回收办公终端不再要求选仓库**：办公终端回收是直接回到办公终端台账的**闲置设备**
  （`it_asset_status = idle`），不进仓库。离职清单里办公终端只选处理方式，并提示「回收到办公终端闲置设备，不进仓库」；
  显示屏 / 非资产物资仍必须选回收目标仓库。
- **机房巡检支持按模板上传表格导入**：巡检页新增「下载模板」与「导入巡检表」。表头固定为
  机房、机柜、检查项分类、检查项、检查方法、结论、实测值、说明；支持 `.xlsx` / `.xlsm` / `.csv`（UTF-8 或 GBK），
  最多 1000 行。导入直接生成一份**已提交**的巡检记录（执行人=导入人），房间/机柜按名称匹配且必须已存在，
  跳过的行按行号列出原因。
- **巡检表可下载**：任务列表与详情都能「下载巡检表」（CSV，任务信息 + 事项明细，含检查人/检查时间）。

### 数据库影响

新增迁移 `database/migrations/20260924_001_rack_placement_category_free.sql`：
删除 `rack_device_placement` 上限制设备类型的 `ck_rack_placement_category` CHECK 约束，
并把 `category` 放宽到 `VARCHAR(64)`。无数据改写，历史上架记录不受影响。

### 验证

- 全新空库按 `bootstrap/` → `migrations/` 顺序初始化，全部迁移执行并 `--verify` 通过；
- 机柜上架一台类型为自定义值（如「光模块」）的设备返回 201，不再出现「设备类型无效」；
- 离职清单提交后：办公终端变为闲置设备、显示屏按所选仓库回补库存，均写入流转记录；
- 用模板生成的表格导入成功并生成已提交巡检记录，下载的巡检表包含全部事项。
  结构回归 161 项、前端类型检查与构建通过。

### 回滚

`git revert` 本提交；数据库侧需要恢复 CHECK 约束时，请先确认没有自定义类型的上架记录。

## v3.0.12

发布日期：2026-09-23

（按用户要求未单独推送 GitHub，已随 `v3.0.13` 一起上传。）

### 变更

- **下拉选项不再写解释性括号**：说明文字一律移到选项下方的提示行。
  - 使用人员领用：型号下拉去掉「自定义（无库存记录，回收时入库）」这一项；
  - 机房设备台账：「上架（由上架操作设置）」→「上架」+ 提示；「型号库（可选，选中后自动带出…）」→「型号库」+ 提示；
  - 设备面板：「型号编码（可留空…）」「端口列表（每行一个…）」→ 标签 + 提示；
  - 工单：「处理结果（标记已解决时必填）」→ 占位符 + 提示。
  仍保留的是**值本身**（如「库存 12」「2U，48 口」）与筛选语义（如「含下级」），它们不是解释。
- **自定义物资与库存型号并存**：型号下拉**始终保留**（可挑仓库里已有的同型号），
  直接输入下拉里没有的型号即视为自定义；出库仓库始终可见（方便对照库存）；
  自定义时出现「品牌」字段（必填）。自定义物资不扣库存，回收时按品牌型号自动补进目录并入到所选仓库。
- **会话改为「无操作超时」**：设置里的「会话时长（小时）」改为「无操作自动退出（小时）」。
  服务端按 `auth_session.last_seen_at` 判定：连续无操作超过设定时长即失效（接口返回 401），
  前端统一处理会退回登录页；有操作时自动顺延（同时续期浏览器 Cookie）。
  前端跟踪真实用户操作，空闲时停止后台轮询——否则每分钟的消息轮询会一直刷新会话，超时永远不触发。

### 数据库影响

无（复用 `auth_session.last_seen_at`，不需要新迁移）。

### 验证

把某个会话的 `last_seen_at` 手工改成 9 小时前（`expires_at` 仍在未来）→ 接口返回 401，
界面点导航即自动回到登录页；把 `last_seen_at` 改成 5 分钟前 → 一次请求后变为当前时间、
`expires_at` 同步顺延 8 小时。结构回归 149 项、前端类型检查与构建通过。

### 回滚

`git revert` 本提交即可（会话会退回"登录后固定 N 小时"的旧规则）。

## v3.0.11

发布日期：2026-09-23

### 变更

使用人员「设备清单」里的人员领用/回收，按旧前端的行为补齐三处差异：

- **领用「类型」改为真实物资类型**。原来看不出领用的是鼠标还是键盘（只有"显示屏 / 非资产设备"两档），
  现在下拉列出台账里的全部类型（显示屏、鼠标、键盘、拓展坞、耳机、USB 集线器、摄像头、电脑…）；
  选择显示屏时按类型编码自动走显示屏通道（数量锁定为 1），其余类型走非资产物资。
- **支持「自定义（无库存记录）」物资**：型号下拉可选自定义，手工填写品牌 + 型号（+ 数量）。
  自定义物资没有出库记录、**不扣库存**，领用时也不会新建目录条目；
  回收时按填写的品牌型号**自动补进目录**并入库到所选仓库。
- **「回收设置」不再常驻在设备信息栏**：改为点击某条物资的「回收」时弹出回收弹窗
  （回收入库仓库必选 + 回收备注），办理离职仍在离职弹窗里逐项选择回收仓库。

### 数据库影响

无。

### 验证

界面实测：把鼠标「罗技 G102 ×2」以自定义方式领用 → 人员名下显示「鼠标 罗技 G102 x2」、库存不变；
点该行「回收」→ 弹窗选择仓库后确认 → 目录自动出现「罗技 / G102」、所选仓库库存变为 2、人员名下清空；
切到「显示屏」类型时型号下拉只列显示屏库存且数量锁定为 1；设备信息栏不再出现回收设置区块。
结构回归 146 项、前端类型检查与构建、脱敏扫描通过。

### 回滚

`git revert` 本提交即可；回滚后领用类型只有两档、无自定义物资、回收仓库常驻在设备信息栏。

## v3.0.10

发布日期：2026-09-23

### 变更

- **修复：导入时选择"更新已存在的设备"无法更新，报「重复设备的处理方式只支持 skip（跳过）或 update（覆盖）」**。
  导入弹窗的开关 `<el-switch v-model="importMode">` 没有声明 `active-value` / `inactive-value`，
  Element Plus 默认往模型里写**布尔值**：拨到"更新"时 `importMode` 变成 `true`，
  请求体发出 `mode: true`，后端只认 `skip` / `update` 两个字符串，于是整次导入被 400 拒绝。
  现在显式声明 `active-value="update"` / `inactive-value="skip"`，开关与请求体都是字符串。

### 数据库影响

无。

### 验证

界面实测：在导入弹窗里选文件 → 拨到"更新已存在的设备" → 确认导入，
已存在的设备被**真实更新**（名称、品牌、占用高度、备注按文件覆盖），同时新增行正常写入；
结构回归新增用例固定开关的两个取值与请求体写法，防止再退回布尔值。146 项回归、
前端类型检查与构建、脱敏扫描通过。

### 回滚

`git revert` 本提交即可（回滚后"更新"开关会再次失效）。

## v3.0.9

发布日期：2026-09-23

### 变更

- **设备类型不再限定在预设枚举内**。网络设备种类多，预设的 12 个类型覆盖不过来：
  - 数据库：迁移 `20260923_001_datacenter_device_category_free.sql` 删除 `ck_datacenter_device_category`
    检查约束，并把 `category` 列从 32 放宽到 64 字符；
  - 后端：新增类型归一化——服务器、网络设备、交换机、配线架、UPS、存储、KVM 等常见写法
    仍会归一化成标准值（例如「交换机」→ `network`），**预设之外的取值按原文保存**，最多 64 个字符；
    导入不再因"类型无法识别"丢弃整行，新增 / 编辑接口同样接受自定义类型；
  - 前端：设备类型下拉改为**按台账里已有的类型生成**（首次使用、台账为空时回落到预设列表），
    并允许直接输入新类型；列表对未知类型直接显示原文。
- 导入说明、`docs/wiki/Device-Ledger.md` 与 `docs/development/datacenter-management.md` 同步更新。

### 数据库影响

删除 1 条 CHECK 约束、放宽 1 列宽度；不改数据，已有取值不受影响。

### 验证

接口实测：以 `category=光模块` 新建设备返回 201；CSV 导入含自定义类型的一行成功入库
（此前该行会被判为"类型无法识别"）；台账类型下拉只列出实际在用的类型（网络设备、光模块）；
结构回归 145 项与导入集成用例（期望值已同步为"自定义类型不再算错误"）、前端类型检查与构建、
推送前脱敏扫描通过。

### 回滚

`git revert` 本提交即可恢复枚举校验；注意列宽不会随代码回滚而收回，
如需完全还原可另写迁移恢复 CHECK 与 32 字符宽度。

## v3.0.8

发布日期：2026-09-22

### 变更

- **修复：机房设备「下载模板」没有跟上新增字段**。模板的表头与示例行是前端写死的数组，
  v3.0.6 新增「用途 / 远程访问地址 / CPU / 内存 / 硬盘」时只改了表单、列表、后端别名与迁移，
  漏了这个模板：导出的 CSV 仍是旧的 10 列，照着模板填的新列会被当成无效列。
  现在模板表头为 15 列（设备编号、设备名称、品牌型号、设备类型、占用高度、SN/ST、固资编码、
  使用人、用途、远程访问地址、CPU、内存、硬盘、状态、备注），示例行同步补齐。
- 导入弹窗里的「可识别的列」说明、`docs/wiki/Device-Ledger.md` 与
  `docs/development/datacenter-management.md` 的字段清单同步更新，避免文档与功能脱节。

### 数据库影响

无。

### 验证

在浏览器里真实点击「下载模板」，拦截生成的 CSV 校验表头与示例行列数一致（15 / 15）；
结构回归新增用例固定模板表头顺序与示例行列数，防止以后加字段再漏模板。144 项回归、
类型检查与构建、脱敏扫描通过。

### 回滚

`git revert` 本提交即可（模板会退回缺少新字段的旧版）。

## v3.0.7

发布日期：2026-09-22

### 变更

- **加固发布前脱敏扫描**：`local-machine-path` 规则原来只匹配双反斜杠写法（JSON / Python 字符串里的转义形式），
  文档里用单反斜杠或正斜杠直接写本机盘符路径会漏过。现在单反斜杠、双反斜杠与正斜杠三种写法都会命中。

### 数据库影响

无。

### 验证

对当前 `main` 与全部 7 个已发布提交重跑扫描均通过；用构造样例确认新规则能拦下
单反斜杠、双反斜杠、正斜杠与大小写盘符四种写法。

### 回滚

`git revert` 本提交即可恢复旧的宽松匹配（不建议，会漏掉单反斜杠写法）。

## v3.0.6

发布日期：2026-09-22

### 变更

- **新增：机房设备的运维字段**。台账表单与列表增加「用途」「远程访问地址」「CPU」「内存」「硬盘」，
  与原有的设备名称、品牌型号、设备类型、占用高度、SN / ST、固资编码、使用人、状态、备注成套；
  Excel 导入同样识别这几个表头。数据库迁移 `20260922_001_datacenter_device_details.sql`
  为 `datacenter_device` 增加 `purpose`、`remote_access`、`cpu`、`memory`、`disk` 五列（均为空字符串默认值）。
- **修复：编辑 / 删除机房设备一直 404**。路由把路径段数写成了 5 / 6，
  而前端用的是 `PUT /api/datacenter-devices/{id}` 与 `POST /api/datacenter-devices/{id}/remove`（4 / 5 段），
  两条路径从来没有匹配成功——表现为"改完点保存没反应"。现已按前端路径修正并补上回归用例。
- **调整：复制只沿用非唯一的配置项**。网络设备复制时带过品牌型号、设备类型、占用高度、用途、
  远程访问地址、CPU、内存、硬盘、使用人，设备编号、名称、SN / ST、固资编码留空手填；
  机柜复制同样只带过所属机房与高度，编码与名称留空。备注一律不复制。

### 数据库影响

新增 5 个字段列（可空、默认空字符串），不改动既有数据；升级时由迁移自动执行。

### 验证

接口与界面实测：编辑设备填入五个新字段后保存成功并回显在列表；复制设备得到的记录带全套配置
但编号 / 名称 / SN / 固资为空，填好编号名称即可保存；结构回归 143 项、前端类型检查与构建、
推送前脱敏扫描通过。

### 回滚

`git revert` 本提交；如果已经执行过迁移，回滚代码后多出的 5 列留着不影响运行（如需清理可手工
`ALTER TABLE datacenter_device DROP COLUMN ...`）。

## v3.0.5

发布日期：2026-09-22

### 变更

- **新增：机房 / 弱电间与机柜可以删除**。此前只能新增和修改。删除会写入操作日志，
  可以填写删除原因，并带引用保护：
  - 机房下还有机柜时拒绝，提示先删除或转移机柜；
  - 机柜内还有已上架设备、或登记着设备端口与链路时拒绝，提示先下架 / 清理；
  - 关联过巡检任务的机房或机柜拒绝删除，避免破坏巡检历史。
  对应接口：`DELETE /api/inspection/sites/{id}`、`DELETE /api/inspection/racks/{id}`。
- **调整：网络设备与机柜的「复制」改为除备注外全部复制**。设备编号、名称、SN / ST、
  固资编码、品牌型号、设备类型、占用高度、使用人，以及机柜的编码、名称、所属机房、高度
  都会带过来，只有备注留空；编号或名称与已有记录重复时，保存会被拒绝并提示
  「数据唯一标识已存在，请检查重复的名称、编码或编号。」，手动改成唯一值即可保存。
  唯一例外：原设备处于「上架」状态时，复制出来的设备状态回到「未上架」——
  上架状态只能由机柜视图的上架操作写入。

### 数据库影响

无（只新增删除接口与界面按钮，表结构不变）。

### 验证

接口层实测：删除仍挂机柜的机房返回 409 并提示原因；删除空闲机房、删除机柜返回 200，
列表随之更新。界面层实测：复制机柜后编码与名称与来源一致、备注为空，直接保存被拒绝
（对话框保持打开），把编码改成唯一值后保存成功并出现在列表。
结构回归 141 项、前端类型检查与构建、推送前脱敏扫描均通过。

### 回滚

`git revert` 本提交即可；回滚后机房与机柜只能新增 / 修改，复制会清空编号与名称等唯一字段。

## v3.0.4

发布日期：2026-09-22

### 变更

- **新增：网络设备 / 服务器「复制」**。台账行操作列增加「复制」，沿用被复制设备的
  型号库、品牌型号、设备类型、占用高度、使用人与备注，只清空设备编号、设备名称、
  SN / ST 与固资编码这些唯一值，状态回到「未上架」，打开对话框时给出补填提示。
  批量上同型号设备时不用再逐项重录。
- **新增：机柜「复制」**。巡检页「机房与机柜」的机柜行增加「复制」，沿用所属机房、
  高度与备注，只清空机柜编码与名称。

### 数据库影响

无（复用既有的新增接口，只是把表单预填好）。

### 验证

结构回归与前端类型检查、构建通过；本地实测：复制网络设备后新设备沿用
「华为 S5731-H48T4XC / 网络设备 / 1U」并保持未上架、编号为空；
复制机柜后新机柜沿用所属机房与 42U 高度、编码与名称为空；保存后均正确出现在列表。

### 回滚

`git revert` 本提交即可，回滚后操作列不再有「复制」按钮。

## v3.0.3

发布日期：2026-09-22

### 变更

- **修复：机房巡检无法新建机柜**。`POST /api/inspection/racks` 一直存在，但前端迁移到 Vue 后
  只剩列表、没有新增入口，表现为「机柜建不出来」。现已补回：
  - 巡检页「机房与机柜」新增「＋ 新增机房 / 弱电间」与「＋ 新增机柜」，
    机柜必须先选所属机房，前端先校验，避免直接撞后端 400；
  - 「巡检模板」新增「＋ 新增模板」，可增删检查项（分类 / 检查项 / 检查方法 / 结论类型 /
    单位 / 正常范围 / 是否必填）；
  - 机房与机柜两张表补上「编辑」（后端 PUT 一直存在），机房表增加「机柜数」列；
  - 机柜视图工具栏增加「新增机柜」，通过 `?tab=sites` 直达机房与机柜页签。
- **修复：巡检模板「检查项」列恒为 0**。列表接口返回的是 `itemCount`，页面读的是 `items`
  （列表不返回明细），改为优先读 `itemCount`。

### 数据库影响

无。

### 验证

结构回归通过；前端类型检查与构建通过；本地起服务实测：新建机房（含弱电间类型）、
新建机柜、新建模板均成功并即时出现在列表，模板检查项数量正确显示（示例：1 / 16 / 14）。

### 回滚

`git revert` 本提交即可；回滚后机房、机柜、巡检模板只能查看，无法在界面新建。

### 已知未实现（不在本次范围）

- 服务表单设计器与审批流程：后端接口存在、旧前端曾有界面，目前前端仍无入口，工作量较大需单独安排。

## v3.0.2

发布日期：2026-09-21

### 变更

- **文档去内部化**：文档、界面文案与数据库设置说明不再出现内部 Git 备份服务的名称，
  统一表述为「内网 Git」；对应的部署文档合并为 `docs/deployment/update-service.md`
  （更新服务、手动更新闸门、发布与回滚）。
- **移除内部基础设施文件**：删除内部 Git 备份库的自建编排与示例配置——那是运维单独维护的
  基础设施，不属于本项目；仓库里只保留更新服务本身。
- **版本说明精简**：只保留 v3.0.x（旧版本已随仓库重建作废）。

### 数据库影响

无。

### 验证

结构回归与前端构建通过；仓库内除运行时标识（部署脚本路径、systemd 单元名、Webhook 签名头）外
不再出现内部 Git 服务名称。

### 回滚

`git revert` 本提交即可；删除的两个编排文件不影响运行中的环境（实际服务不在部署目录内）。

## v3.0.1

发布日期：2026-09-21

### 变更

- **修复：使用人员名下物资不显示类型**。服务端 `/api/state` 下发人员名下的显示屏与非资产物资时
  只带 `typeId`、没带 `typeName`，前端只能统一兜底成「物资」。现在两者都带上类型名
  （显示屏 / 鼠标 / 键盘 / 拓展坞 / 鼠标垫 / 笔记本支架 / 键鼠套装 / 有线键鼠…），
  类型字典里查不到时显示屏回落「显示屏」、非资产物资回落「非资产设备」。
  影响面：使用人员列表「名下设备」列、设备弹窗、CSV 导出。

### 数据库影响

无。

### 验证

本地库实测 160 条人员物资（104 显示屏 + 56 非资产物资）全部带类型名、0 条缺失；
浏览器「名下设备」列不再出现「物资」；结构回归新增断言守住该字段。

### 回滚

`git revert` 本提交即可，接口字段为新增，旧前端不受影响。

## v3.0.0

发布日期：2026-09-21

### 变更

- **更名**：项目更名为 **OMbox · 运维（O&M）盒子**；系统设置里的默认系统名同步改为
  「OMbox运维（O&M）盒子」（`database/bootstrap/19_auth_and_settings.sql` 的 `app_name` 初值）。
- **仓库重建**：以单个初始提交重建仓库，历史中不再保留早期提交里的真实业务数据；
  版本号从 v3.0.0 重新起，旧版本标签全部作废。
- **README 重写**：按 OMbox 重新介绍定位、能力、部署方式与文档入口。
- **测试数据约定**：测试与示例一律使用虚拟信息（张三 / 李四 / 测试部门 / IT-PC-01 等），
  不引用真实人员、设备编码、组织与仓库名称。

### 数据库影响

无结构变更。默认系统名只影响**全新部署**；已有库沿用 `system_setting.app_name` 里的现值，
需要改名时在「设置 → 系统设置 → 系统名称」修改。

### 验证

脱敏扫描通过；单元与结构回归通过；`vue-tsc` 与 `vite build` 通过；按 Docker 初始化流程
重建空库核对：仅权限字典与参考数据有内容，人员、终端、账号、审计日志等业务表均为 0 条。

### 回滚

历史已重建，无法 `git revert` 回到 v2.x；回退需部署旧版本包并从备份恢复数据库。
